import PhaseClient from './PhaseClient'

export const metadata = {
  title: 'Magic Engine — Roadmap Dictionary',
  description: 'Internal phase dictionary: search phases, view dependencies, track git activity',
}

export default function RoadmapPage() {
  return <PhaseClient />
}
