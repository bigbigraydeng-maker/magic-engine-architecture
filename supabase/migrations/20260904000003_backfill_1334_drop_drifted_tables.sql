-- ============================================================================
-- #1334 补登记（三）：拆掉两张反向漂移表
-- ----------------------------------------------------------------------------
-- keywords / semrush_usage_logs：生产 2026-05-29 经账本条目
--   20260529132625 archive_drop_manual_keyword_tables 归档后删除，但删表动作
--   从未进 git，导致仓库 baseline 建的它们在从零重建后残留。此处补删，
--   使终态与生产一致。IF EXISTS，生产重跑为 no-op。
--
-- 排在文件（一）（二）之后：确保任何引用这两张表的早期 migration 都已重放完毕。
-- ============================================================================

-- ============================================================================
--  拆掉两张反向漂移表（生产 2026-05-29 已删，仓库 baseline 仍在建）
-- ----------------------------------------------------------------------------
--  生产账本 20260529132625 archive_drop_manual_keyword_tables 把这两张手工
--  keyword 表归档后删除，但删表动作从未进 git，导致仓库 baseline 建的它们在
--  重建后残留。此处补删，使终态与生产一致。IF EXISTS，重跑无害。
-- ============================================================================

DROP TABLE IF EXISTS public.keywords            CASCADE;
DROP TABLE IF EXISTS public.semrush_usage_logs  CASCADE;
