# ME 会员体系 v2 重新设计 · 进度记录（多轮 PM 对话滚动更新）

> **别名"张居正"**：这场设计讨论对应的 skill（[`.claude/skills/me-membership-tier-design/`](../../.claude/skills/me-membership-tier-design/SKILL.md)）2026-09-02 起有了别名"张居正"，任意窗口打 `@张居正` / `召张居正` 即可续聊，等同 `/me-membership-tier-design`。取名逻辑同"张良 = me-platform-tier-gate"惯例。

# Status

**DESIGN IN PROGRESS — 非任务合同，不授权实施。** 这是产品负责人（Ray）与 Claude 的多轮设计讨论记录，用于跨会话续聊，不是 Issue、不需要 Build Control、不能直接拿去 GO BUILD。等设计定稿，再回填成 #1273-1276 的修订，或另开新 Issue。

**怎么用这份文档**：每次开新会话继续聊，先读这份文档"已定"和"待定"两节，接着上次的"待定"往下问，不要从头再问一遍。每敲定一条，随手把它从"待定"搬到"已定"，并写清楚原因（不止是结论）。

---

## 背景

2026-08-31 产品负责人拍板了 ME 会员制度 v1.1（免费 / $39 / $199 / $499 / 定制），已经立项的四张票是 [#1273](https://github.com/bigbigraydeng-maker/magic-engine/issues/1273)（免费站生成器）、[#1274](https://github.com/bigbigraydeng-maker/magic-engine/issues/1274)（免费档生命周期）、[#1275](https://github.com/bigbigraydeng-maker/magic-engine/issues/1275)（$39 订阅）、[#1276](https://github.com/bigbigraydeng-maker/magic-engine/issues/1276)（扫街预建站），全部还是 SPEC DRAFT。

这四张票定义的免费/$39 档是"网站阶梯"（先给网站，再解锁域名/邮箱），完全没碰到 6 大支柱（SEO / 社媒 / 广告 / 口碑 / AI 可见度 / 竞品）。2026-09-01 起，产品负责人要重新设计**这条阶梯之上、6 支柱怎么分布到各档**的服务内容和定价——**框架不变（网站阶梯是地基），内容和定价要重新设计**。

$199 / $499 目前完全没有 spec，`docs/registry/pricing-playbook-enterprise-fde.md` 明确写着"报价材料禁止承诺这两档，除非订阅契约先落地"。

---

## 获客渠道 → 客户画像（2026-09-02）

**获客路径**：线上——PM 本人（大瑞老师）个人 IP 讲"海外流量"，覆盖小红书/LinkedIn/抖音；线下——演讲、商会活动。转化路径是**培训 → 体验 → 诊断 → 客户**，不是"投广告转化陌生人"。

**倒推的核心画像特征**：AU/NZ 华人中小生意主，靠商会/华人网络认识彼此（高密度熟人网络），数字营销基础一般，买单前先经过培训/诊断建立信任，同时要被中文圈（小红书/微信）和主流市场（Google/Facebook）两边看到。

### 四个具体画像（对应现有客户构成：CTS、Park Homes、双子留学 / Oztop、HBay）

| 画像 | 例子 | 生意逻辑 | 日活驱动 | 几乎不碰的东西 |
|---|---|---|---|---|
| **信任驱动型服务生意主** | 旅行社（CTS）、地产经纪（Park Homes/Roman）、留学中介（双子）| 咨询成交，一单几千到几万，靠信任 | **CRM + 留资提醒**（私信/邮件里有没有新客人）| SEO 仪表盘、关键词报告——太技术 |
| **产品/门店型生意主** | 地板经销（Oztop）、瓶装水（HBay）| 靠"被搜到 + 看着靠谱"驱动进店/下单，相对标准化 | **口碑监控（GBP 评分）+ 本地 SEO**，周活不是日活 | CRM——客户多是一次性路人，不是靠关系维护 |
| **新手/观望型**（免费·$39 典型用户）| 刚做完培训/诊断，还没决定认真投入 | 尚未建立使用习惯 | **不会主动打开任何仪表盘** | 几乎所有主动登录型功能 |
| **想省心的规模化老板**（定制档典型用户）| HBay 那个量级 | 生意已经跑起来，要的是有人管不是工具 | 一个月一次月度策略会 | 日常操作型功能全部不碰 |

### 综合洞察：产品默认假设"每根支柱配一个仪表盘"是错的

画像三**明确不会主动打开仪表盘**（上表标注"不会主动打开任何仪表盘"）；画像二按上表定义是"口碑监控（GBP 评分）+ 本地 SEO 驱动**周活**"，即仍会周期性登录，只是频率比画像一低——**"根本不会打开仪表盘"这个结论目前只对画像三成立**，画像二收窄为"登录频率低、周活不日活"，除非后续有真实使用数据证明画像二也不主动登录，否则不外推到画像二。即便收窄后，画像二 + 三加起来仍可能是获客渠道里最大的两块，原设计里"主动推送/通知"只是分散在各支柱里的可选项，值得考虑**提升成一条跨支柱基础设施能力**：

- 免费/$39：每月一次"网站访问量 + 新联系人"邮件摘要，不用登录就能感知价值
- $199：加上"口碑/竞品关键变化"提醒（评分变了、竞品降价了）
- $499：更即时的推送（差评、广告异常、竞品大动作）

**层级判定（本节内已完成，非待办）**：`docs/specs/2026-06-05-mcp-phase2-product-design.md` §1.2/§1.3、§4.1-4.3、§5.2、§8 已经设计并拍板了同一条"跨支柱推送基础设施"——`clients.notification_email` 字段 + 后台 UI、`notification_log` 表（`channel` 字段本就预留 `'email' / future 'wechat'`）、周报渲染模板，PM 已拍板"周一 + 周五各一封"频率、微信渠道"暂不做"。跑 [me-platform-tier-gate](../../.claude/skills/me-platform-tier-gate/SKILL.md) 判定结果（Inline 模式）：

> [跟班 · Tier] 这是 **L1 平台基础设施**（跨 6 支柱的通知/摘要触达层，不专属某一支柱）——已在上述 2026-06-05 spec 里拍板为共享设计，本次只是在这层之上按会员档位加分层触发规则，不新增能力线、不需要登记 candidates。
> [跟班 · 判据] 换客户测试 ✓：`notification_email`/`notification_log`/触发规则全部按 `client_id` 参数化，没有一条默认行为只对首个客户成立；换行业测试 ✓：推送频率只随会员档位变化、不随行业变化，不存在"只对某行业成立"的默认值；红线 1（禁包装升级）✓ 未新造"XX Intelligence"名义；红线 2（禁客户/行业事实进 shared runtime）✓ 未写入客户名或行业硬编码。
> [跟班 · 结论] 归位既有 L1，无需登记候选，可以继续往下写方案——不允许绕开这份既有拍板重新设计一套并行的通知渠道。是**复用**该既有设计（大概率）、**修订**其范围（比如把"周报"扩展为按会员档位分级的推送触发条件/频率），还是有充分理由**废弃**重来，留给下方"待定"项继续讨论。

**渠道现状核查（2026-09-03 三次修正，重新按当前仓库状态跑 Reuse/Build-vs-Connect 判定）**：2026-06-05 spec 当时把邮件渠道锁定成 SendGrid，但那之后仓库已经落地了另一套事务邮件能力——`docs/STATE.md:195` 把 **Resend** 登记为"全部事务邮件"供应商，`docs/ENV.md:145` 已登记 `RESEND_API_KEY`，`src/lib/email/sender.ts` 已提供统一的 `meMailFrom`/`ME_MAIL_TO_ADDRESS` 封装，且 `src/app/api/cron/weekly-seo-report/route.ts:38-60` 已经用 `Resend` SDK + 这套封装每周给客户发周报。也就是说邮件发信能力**不是空白**，`admin-key-expiry/route.ts:5-6` 那句"SendGrid not yet built"只是一处没跟着 P1 → P22 之后的供应商切换更新的过期注释，不能当作"仓库仍然选型 SendGrid"的证据。**渠道选型改为已按现状复核**：邮件走**已建成的 Resend + `src/lib/email/sender.ts`**（不新增 SendGrid SDK / 不新增第二套邮件供应商），微信按 §1.3/§8 拍板"暂不做"不变。真正要新建的实施量收窄为：`clients.notification_email` 字段 + 后台 UI、`notification_log` 表 migration、按会员档位分层的触发规则、复用 `weekly-seo-report` 同款 Resend 发信路径的推送渲染模板/cron 路由——这些要计入 GO BUILD 工作量，但发信通道本身是复用而非新建。真正待定的只是"要不要在这条还没建的推送链路上按会员档位加分层触发规则"。

---

## 已定（截至 2026-09-01）

### 总体框架
- **不推翻 #1273-1276 的地基**，免费/$39 档的"网站+域名+邮箱"部分保留；这次重新设计的是**6 支柱在各档怎么分布 + 定价 + 具体额度**
- CRM / 邮件读取等工具走 **Connector 原则**：会积累数据、越用越准的判断层（学习型能力）—— ME 自己造（L1）；其他一切能买/能接的 —— 用 Connector 接，是"ME 帮你配好的工具"，不代表 ME 要重新发明它。跟已冻结的公司级战略决策 [#1131](https://github.com/bigbigraydeng-maker/magic-engine/issues/1131) 一致（ME 不做完整 CRM 公司，只做 CRM 之上的 Marketing-to-Sales 判断层）

### 免费档
- 定位：**数字基建，不是支柱能力**。6 支柱要等基建就绪后才能展开
- 功能：二级域名网站（一次性开发，边际成本近零）+ 每月一次网站流量报告 + 每日查收邮件内容
- **"每日查收邮件内容"是新能力，还没建过**——已记为待登记候选（不是 CTS 那种单客户定制邮箱托管，是面向所有自助客户的通用产品功能），登记判定推迟到设计定稿后再走 [me-platform-tier-gate](../../.claude/skills/me-platform-tier-gate/SKILL.md) 完整流程
- 名额策略未最终拍板（见"待定"）

### 跨档设计原则：agentic 是"从 $39 起就在跑的圆圈"，不是某个价位才解锁的开关（2026-09-02 修正）
- **错误理解（已推翻）**：不要把"AI 自主执行 vs 只给建议"当成某个价位（比如 $199 或 $499）才解锁的二元开关
- **正确模型**：从 $39 起 agent 就已经在跑了，档位越高不是"第一次有了 agent"，是**同一个 agent，圈子越套越大**。三个独立的轴：
  1. **宽度**：这根支柱在这个档位有没有（存在/不存在）
  2. **深度**：跑得多勤（频次）、能干多少事（权限范围，比如预算笼子大小）
  3. **综合实现能力**：花多少资源去实现——链接多少数据源、分析多深、调用多贵/多聪明的模型，这决定了离"真正对结果负责"的完整 IMPACT 闭环有多近
- 具体应用：广告支柱不是"$199 只给建议、$499 才有自主执行"，而是"$199 广告 agent 就已经能自主执行了，只是预算笼子小（$1,000/月硬顶）；$499 是同一个 agent，预算笼子改由客户自己配置真实预算（不是 ME 定一个更大的数字），差异在判断更深"（细节见下方矩阵）

### $39 档
- 核心原则：**"麻雀虽小，五脏俱全"**——哪怕功能窄，也必须是一个完整 IMPACT 闭环（发现问题 + 客户能自己动手 + 能看到结果变化），不能只给诊断不给"能自己修"的东西
- 支柱不强制每档都有，按**"AI 能不能独立判断 + 有没有对外发布/花钱风险"**排序进场，顺序由产品经理故意设计，用来引导客户往上升级：
  - **AI 可见度**：月度监控（1 个引擎，ChatGPT）+ 客户能自己动手的小任务（具体任务待定）
  - **竞品监控**：只读监控，无发布动作
  - **SEO**：轻量强度（弱于 $199），参考 Semrush 的打法（细节待定）
  - **口碑**：不止 GBP，还有行业专属口碑网络（餐饮、旅游各自的点评平台），前提是客户自己去授权绑定——这些行业专属 Connector 以后逐个接入时按 L2 Playbook 登记，非阻塞
  - **社媒（内容工厂：图片/视频）**：从 $39 起有，但数量/强度受限——图片 10-15 张/月、视频剪辑 2-3 条/月。**视频 = 剪辑客户上传素材，不是 AI 从零生成**（原因见下方"跨档设计原则"）
  - **广告**：$39 不给，从 $199 才开始有

### 跨档设计原则：视频功能默认走"剪辑客户素材"，不是"AI 生成"
- **原因**：AI 从零生成视频依赖完整 master brief 才能写出好提示词，在客户信任/数据还没建立起来的低档位是"抽盲盒"，质量没保证；而且 AI 生成视频（Muapi 类）单条真实成本 $0.6-4，是全场景唯一真正烧钱的项。剪辑客户自己上传的真实素材是确定性处理（掐头去尾+字幕+模板拼接），不用 AI 猜画面，稳定性更高，成本也低得多（是服务器渲染时间，不是按次调用生成式 API）
- **复用现状核查（2026-09-02 修正，别把 CLI 原型当已上线能力）**：客户免登录素材上传管道已上线，但只是把成片存成未分析的 `client_assets`；生产里唯一接了这条路径的入口是 `src/app/api/clients/[id]/content-factory/[postId]/video/route.ts`，它接收的是**人已经剪好的成片**，只做落库（写 `source_video_url`），不做任何剪辑。"边走边录"自动剪辑（`renderWalkTalkProof`，`src/lib/factory/walk-talk-proof.ts`）**没有接入任何生产 worker 或 API 路由**，目前只被 `scripts/render-walk-talk-proof.ts` / `scripts/render-walk-talk-from-srt.ts` 两个本地 CLI proof 脚本调用。也就是说"客户传素材，ME 剪"这条 $39 承诺里"ME 剪"的自动化部分（认领客户原片 → 剪辑 → 字幕 → 模板拼接 → 交付）**还没有产品化链路**，只有 PoC 脚本；GO BUILD 前要把这条从 CLI 原型接成真正的生产 worker，不能按"基本不用新建"排期
- 没素材可传时的兜底机制（要不要留 AI 生成保底、要不要设上传门槛）留给后续产品设计阶段决定，不占用这次分档设计的时间
- **这条原则默认对 $199/$499 也生效**，除非后面讨论到更高档位时明确要引入 AI 生成来补客户素材不足

### $39 档成本测算（真实数字，不是拍脑袋）
- 目标毛利 ≥80%，但 **80% 不能只算 compute 成本**——Stripe 手续费（2.9% + $0.30/笔）在 $39 这么薄的档位上就已经占了 3.7%，还没算客服/获客/基础设施
- 真实成本基准（引用 Claude 跨会话 memory `reference-ai-api-unit-costs-2026-08`，2026-08 公开定价，不在本仓库内，下次续聊时 Claude 会自动带上）：诊断/监控类 AI 调用每次 ~$0.05-0.30 NZD；图片 ~$0.03-0.05 NZD/张；**视频是全场景唯一真正烧钱的项目**，最低配（480p·6 秒）也要 ~$0.6-1 NZD，720p·15 秒能到 $4+
- **基础额度包**（compute 成本约 $1.5-3/用户/月，**compute-only contribution margin 92-96%，不是完整毛利**）：4 项监控（AI 可见度/竞品/SEO/口碑）各每月 1 次 + 图片 10-15 张/月 + **视频剪辑 2-3 条/月**（剪辑客户自传素材，不是 AI 生成——成本是 worker 渲染 + 存储 + 传输，按次调用生成式 API 的 $0.6-4/条不计入这里，单条剪辑成本远低于此）；加上 Stripe 手续费（$39×2.9%+$0.30=$1.431/笔）后，真实毛利约 **88.6%-92.5%**——且这仍未计入视频剪辑本身的 worker 渲染/存储/传输成本，也未计入客服和基础设施，完整毛利会比这个数更低
- **"挖爽感"加码版**（compute 成本约 $3-5/用户/月，同样是 compute-only 口径）：AI 可见度监控从月度提到**周度**（这是"周度提频"唯一生效的地方，基础额度包仍是月度）+ 每月额外送 1 条 AI 生成的最低配视频（480p·6 秒，区别于基础额度包"剪辑客户素材"的那 2-3 条）+ 每月一份可分享的"增长战报"一页纸（复用诊断报告生成器，几乎零成本，顺带是免费传播钩子）；加上 Stripe 手续费后真实毛利约 **83.5%-88.6%**，同样未计入 worker/存储/传输/客服/基础设施
- 技术实现：**复用现有 MTC 账本**（`src/lib/mtc/`），不新造配额系统——把"一次性买 MTC"扩展成"$39 订阅每月自动发一批 MTC"，这本来就是 #1275 的范围；视频/图片复用现成的视频工厂后台，不重造

---

## $199 / $499 六支柱完整矩阵（2026-09-02 定稿，2026-09-02 按真实工程现状二次修订"诚实上线"版）

**修订原则（PM 拍板 2026-09-02）**：按现在真实能兑现的范围上线，做不到的写进路线图，不写进定价承诺。

| 支柱 | 免费 | $39 | $199 | $499 |
|---|---|---|---|---|
| **AI 可见度** | 不给（基建阶段）| ChatGPT 监控（现成，基础额度是月度）+ llms.txt 自动生成/提交（客户能自己动手的小任务）；周度提频仅"挖爽感加码版"可选，不是基础档承诺 | ChatGPT + Gemini 监控（现成，本来就在跑）| 标"多引擎监控"但 **Claude/Perplexity 需先接进 cron 才能兑现**——上线前置工程，见下方"能力现状核查" |
| **竞品** | 不给 | 只读月度监控（可行业级批量发现，也可具体公司追踪）| 追踪频率提高 + 更多维度 + 竞品广告库透视（Meta Ads Library 公开数据，见下方新功能）| 预警：竞品有重大动作主动提醒 |
| **SEO** | 不给 | 月度快照：几个种子词的 Volume/KD/CPC（DataForSEO `search-volume.ts`）+ 轻量技术扫描 | 周度刷新 + 关键词范围扩大 + 竞品关键词缺口分析（`labs.ts`）+ 技术修复自动执行 + 基础内容（1 篇/月）| 日度 + 全量关键词宇宙 + 内容规模化（多篇/月），对齐 Semrush 强度 |
| **口碑** | 不给 | GBP + 行业专属网络监控（只读，需客户授权绑定）| AI 草拟回复，人工确认后发 | 常规好评/中性评论自动回复，负面仍走人工，覆盖渠道更全 |
| **社媒** | 不给 | Facebook（成熟，organic 发帖，跟广告发布是两套不同代码）+ TikTok（能发，待确认 ME 应用过审状态）；客户传素材，ME 剪（2-3 条/月——**剪辑管道目前只有 CLI proof 脚本，没接生产 worker，是 GO BUILD 缺口，不是"基本不用新建"**）；小红书任何档位都只排版不自动发 | 同上平台，剪辑数量提升 + 排期日历 | 同上平台，完整内容日历 + 更高产出量，**素材不够时可引入 AI 生成补缺口**（例外，见上方"跨档设计原则"）|
| **广告** | 不给 | 不给 | **自助投放 Agent，预算笼子 $1,000/月硬顶**（目标形态——见下方"四个广告主仓库现状核查"，接线/安全缺口补齐前不能上线；**月度累计上限本身尚未实现，见下方"月度累计预算闸"缺口，是独立于四个广告主之外的额外上线阻塞项**）| 同一个 agent，**笼子 = 客户自己设的真实预算**（不是 ME 定一个更大的数字），差异在**判断更深**（更贵的模型 + 更多数据信号去判断要不要花更多），同样受下方缺口清单及"月度累计预算闸"约束 |
| **CRM**（不算支柱，跨支柱基础设施）| 不给（客户还没积累往来数据，给了也没意义）| 不给 | 基础功能（联系人/阶段/跟进）| 同 $199 + 自动阶段判断（**旅游客户先能用，其他行业要等 #1121 泛化后才能卖**）|
| **邮件**（不算支柱，免费档已定）| Microsoft 邮箱 touchpoint 同步（谁联系你，记时间线，**不读内容**；Gmail 不支持）| 同左 | 同左 | 同左（内容提取需接线 `mailbox-evidence`，列路线图）|
| **效果归因反馈**（新功能，补 IMPACT 的 Check/Tune，见下方）| 不给 | 不给 | 轻量月报：这个月的动作有没有见效（复用 CRM `attribution.ts`）| 同左，更深 |

### 能力现状核查（2026-09-02，五个领域逐一核实，别在定价页承诺工程还没跟上的东西）

| 领域 | 现状 |
|---|---|
| AI 可见度/GEO | 生产真正每周自动跑的只有 **ChatGPT + Gemini** 2 个引擎（`src/lib/ai-tracker/orchestrator.ts` 默认值）；Claude/Perplexity 调用代码都写好了但**只能手动触发，没接进 cron**；`docs/STATE.md` 里写"GEO baseline 支持四引擎"是**过时/错误信息**，实际 `geo-baseline/provider.ts` 只接了 OpenAI 一家，代码自己注释"这是一次性接线，不是平台能力" |
| SEO | 比预期成熟——`site-audit-cron`（每天）、`keyword-snapshots-weekly`（每周）、`site-audit-weekly`（每周）、`seo-patrol-daily`（每天）都已经在生产跑，天然自带频次分层，不用新开发，只是权限问题 |
| 社媒 | 真正原生自动发布只有 **Facebook**（成熟）+ **TikTok**（能发，但 ME 的 TikTok 应用没过平台审核前，发出去的内容只有自己能看到——平台限制不是 ME 能绕过的）；Instagram 等看客户自己 Publer 账号连了什么；**小红书没有官方 API，任何档位都不能自动发**，只能帮客户排版让客户自己手动上传；LinkedIn 目前只服务创始人个人账号，非客户通用能力；**"客户传素材、ME 剪"这条 $39 承诺的自动剪辑管道也没接生产**——见下方"审阅修正"|
| CRM | 架构通用（按 `client_id` 参数化，路由无硬编码），但**出行日期解析、Meta 表单 consent 判定这两处业务规则是从 CTS 反推的**，换非旅游客户不一定适用——正是已挂着的 [#1121](https://github.com/bigbigraydeng-maker/magic-engine/issues/1121)（CTS 硬编码脱敏→行业打法配置）要解决的事 |
| 邮件 | 客户能自助连自己的 Outlook/M365 邮箱（Settings 里有入口），每小时同步一次，抓"谁给你发邮件"写进 CRM 时间线；**只支持 Microsoft，没有 Gmail/IMAP**；真正"读懂邮件内容"的解读模块 `mailbox-evidence/interpreter.ts` 已经写好但**零调用方，是造好没接线的孤儿模块** |

### 这轮新增的 4 个功能（复用已有能力，2026-09-02 采纳 3 条 + 1 条另记候选）

1. **llms.txt 自动生成 + 提交** —— 填上 $39 档"AI 可见度小任务"的具体答案，已有试点验证过（[#1179](https://github.com/bigbigraydeng-maker/magic-engine/issues/1179)），低风险、可撤销、AI 能独立判断，放 $39
2. **效果归因反馈** —— 补上矩阵里一直缺的 IMPACT Check/Tune 环节（之前所有支柱都停在"Act"，没有"做完有没有效"的反馈），复用 CRM 已有的 `attribution.ts`，$199 起给
3. **竞品广告库透视** —— 用 Meta Ads Library 公开数据看竞品在投什么广告，不需要客户授权（零风险），放竞品支柱较低档位
4. **本地同行批量发现** —— 复用 `dataforseo/business-listings.ts`，PM 明确这**不是独立功能，就是竞品支柱本身**（可以是行业级批量扫，也可以是具体公司追踪），已合并进竞品支柱定义，不单独列

**广告笼子设计原则（2026-09-02 定稿，2026-09-02 补充护栏边界）**：**不限制功能，只限制预算**——笼子里客户能用的功能就是 Meta/Google/TikTok/ChatGPT 平台原生提供的那些，ME 不额外阉割"能不能开新广告系列"这类功能白名单。但**预算硬顶只是消费上限，不是唯一的安全阀**：预算内仍可能出现投错地域、受众被平台自动放宽、错误文案直接触达客户等事故。现有 `src/lib/ads-strategy/ad-draft.ts` 和 `src/lib/meta/ad-publisher.ts` 采用的"暂停创建 → 回读 → 人工批准 → 激活"模式必须作为独立于预算之外的护栏保留——动作授权范围、目标/创意回读校验、审批闸门、可回滚、紧急暂停按钮，都不能因为"不限制功能"这句话被理解为连审批/回读环节也一起去掉。

**广告 $499 vs 定制档的差异**：不是自动化程度不同（两者的"自主"范围一致，见下方澄清），是**配置方是谁**——$499 客户自己通过自助 UI 配置；定制档由 ME 的人帮客户摸清业务后配置。风险提示（未来做自助投放工具时要处理）：客户自己配错笼子（预算单位搞错、受众设太宽）时，钱烧出去之前没有人会发现，需要新手引导/合理默认值/紧急暂停按钮这类护栏。

**"自主执行"的边界澄清（2026-09-02 补充，消解与"人工批准"护栏的表述冲突）**：这里的"AI 在预算笼子里自主执行"指的是 agent 能自主完成**判断 + 起草**——不用人告诉它该不该投、投多少、投给谁，它在预算笼子和既有护栏范围内自己拿主意、自己生成待激活的广告草稿。但**激活动作本身不豁免**上面"广告笼子设计原则"里的"暂停创建 → 回读 → 人工批准 → 激活"模式：每一次新建/变更广告系列，AI 起草后仍停在暂停态，仍需人工回读确认才能真正花钱激活。也就是说，"自主"消灭的是"客户/PM 要手动想策略、手动配置广告"这一步，不消灭"钱花出去之前有人看一眼"这一步——$499 与定制档在这一点上完全一致，唯一差异仍是配置方是谁。

**四个广告主的仓库现状核查（2026-09-02，别在定价页承诺工程还没跟上的东西；2026-09-02 补充完整动作链缺口清单）**：

| 平台 | 现状 | GO BUILD 前必须补的完整动作链缺口 |
|---|---|---|
| Meta | **代码具备，但接线未完整，暂不算首投可上线**——`src/lib/meta/`（`ad-publisher.ts` + `ads-manager.ts` + `adsets.ts` + guardrails）能真正生成/发布广告，护栏骨架也在 | 五条缺口全部要在首投前补齐：① 起草这一步**没有任何调用方**（`docs/ROADMAP.md:129` P21.J.M4）——没有 AI/UI 真的产出草案，链路空转；② 素材上传未接（`docs/ROADMAP.md:130` P21.J.M5）——`imageHash`/`videoId` 要人先传到 Meta 才有；③ `approveDraft`（`draft-and-gate.ts:174-207`）激活前只重读数据库里的旧 payload，**不重新回读 Meta 现状**，草案建好后中间被平台改过（受众放宽、创意变化）发现不了；④ 买家可见文案缺失（`launch-readback.ts:294-307` `creative_without_buyer_text`）严重级是 `warn`，**不会阻止激活**，跟"钱花出去之前有人看一眼"的护栏原则不符；⑤ **批准/否决不是原子认领**——`approveDraft`（`:179-209`）和 `rejectDraft`（`:213-235`）都是先读状态、再无条件写状态，两人同时点击批准/否决，或有人否决一条已经 `active` 的草案，会出现 Meta 广告已经激活并持续花钱、账本和审批页却最终显示 `rejected` 的资金安全事故（`rejectDraft` 甚至连当前状态都不检查，不需要并发也能触发，见 `docs/ROADMAP.md:185-186`）；此外 `ad-draft.ts` 只固定支持 `lead_form` / `video_thruplay` 两种打法——要覆盖 Carousel、Collection、Advantage+ 等更多广告形式还要补齐打法模板 |
| Google Ads | `src/lib/google-ads/` 只有管理已有广告（暂停/改预算）+ 关键词研究 | campaign 创建、ad group 创建、creative（RSA/展示广告）创建、素材上传、投放后回读、激活验证、"暂停→回读→审批→激活"护栏模式——目前全部缺失 |
| TikTok Ads | `src/lib/tiktok-ads/` 同 Google，管理能，创建不能 | 同 Google：campaign/ad group 创建、creative 创建、素材（视频/图片）上传、投放后回读、激活验证、护栏模式——目前全部缺失 |
| **ChatGPT Ads** | **真实存在**——OpenAI 2026-02-09 上线，2026-05-05 开放自助 Ads Manager、取消 $50,000 最低消费门槛，现在 CPM ~$25，小生意能直接自助投 | **ME 仓库里完全没有这个 Connector**——campaign/ad group/creative 创建、素材上传、预算与投放控制、投放后回读、激活验证、护栏模式，全部要从零建 |

**月度累计预算闸（2026-09-02 四轮审阅新增，独立于上表四个广告主之外的额外上线阻塞项）**：`$1,000/月硬顶`目前只是矩阵表里的目标形态，工程上并不存在——`src/lib/ads-strategy/ad-draft.ts:77-112` 的 `validateDraft` 只检查**单条草案**「每天最多 `MAX_AUTO_DAILY_BUDGET`（$50）× 最多 `MAX_DURATION_DAYS`（30 天）」，没有任何地方核算同一客户在一个自然月内跨多条草案 / 跨 Meta·Google·TikTok·ChatGPT 多平台的**累计**消费；`supabase/migrations/20260808000003_me2_execution_kernel_v1.sql:39-42` 里给周期上限预留的 `spend_cap_per_period_usd` / `spend_cap_period` 字段明确注释为 **`RESERVED · NOT ENFORCED`**，v1 只实现了单次上限。也就是说，客户只要连续批准多条草案，或者同时在多个平台各投一条，每条都能各自通过 `validateDraft` 的单条检查，但月累计可以远超 $1,000——这个硬顶目前是假的安全感。**GO BUILD 前必须补齐且列为独立阻塞项**：① 跨 campaign / 跨广告平台的累计消费核算（把 `spend_cap_per_period_usd` 从 reserved 字段落到真正的 enforcement）；② 明确"月"的窗口语义（自然月 vs 客户订阅周期滚动 30 天，需与计费周期对齐）；③ 明确币种语义（客户账户货币 vs 统一 NZD/USD 折算，尤其 AU/NZ 双市场场景）。四个广告主表格里任何一项接线缺口补齐，都不代表这条月度累计闸也补齐了，两者必须分开验收。

四档设计目标形态不受影响，但上表每一项缺口都是定价页写"覆盖 Meta/Google/TikTok/ChatGPT 原生广告能力"之前必须补齐的工程量——**Meta 也不例外**：`ad-publisher.ts` 等核心发布代码具备只说明"能力存在"，起草没有调用方、批准前不重新回读、买家文案缺失不阻断激活、批准/否决不是原子认领，这四条接线/安全缺口不补，"自助投放 Agent"这条链路要么空转（没有调用方产出草案），要么在真投放时可能按过期状态把钱烧出去，要么出现账本显示已否决、Meta 广告却仍在花钱的资金安全事故。**在这五条缺口补齐之前，$199/$499 定价页目前对任何广告主都不能承诺"自助投放 Agent"或"从零创建广告系列"**；Google/TikTok 现在只能承诺**广告管理**（暂停/改预算）。**ChatGPT Ads 不能出现在任何承诺列表里**——仓库里连管理（暂停/改预算）用的 Connector 都不存在，客户买了之后连暂停投放都做不到；ChatGPT 要等 Connector 从零建完才能和 Meta/Google/TikTok 一起出现在定价页。

### 记录但不在本次讨论范围：开放第三方 developer 入驻共建

PM 提出未来新功能可以走"第三方 developer 共建"的路子（类似插件市场/开发者生态）。这**不是**"某功能归哪层"的技术分类问题，是全新的商业模式决策（收费分成、第三方代码安全审核、客户数据访问边界），量级等同"ME 要不要从 Connector 抽佣"，已按 Low bar 登记进 [`docs/registry/platform-candidates.md`](../registry/platform-candidates.md)，**需要单独展开一次讨论，不在这次会员分档设计范围内处理**。

## 定制档：不重新设计服务内容，重新定义卖点

**结论（2026-09-02 四轮审阅收窄，同日已同步修订落地）**：定制档服务内容本身**不需要重新设计**——跨支柱综合判断 + 月度策略会 + VI 一致性协调这套"卖点"沿用已验证的 Enterprise FDE playbook（[`docs/registry/pricing-playbook-enterprise-fde.md`](../registry/pricing-playbook-enterprise-fde.md)：一次性 Setup $3,000 + Base 月费 $2,500 起 + 广告管理费边际阶梯）没有问题。**"沿用"不等于"照单全收现有 ChatGPT 承诺"**：该 playbook 原先第 17 行把 Meta / Google / ChatGPT 三个平台的账号接管与**首次投放**写进 Setup Fee、第 34 行把 ChatGPT 列为广告管理费可选平台之一——这曾直接违反本文件上方"四个广告主仓库现状核查"的结论。**这处矛盾已解决**：playbook 第 17、34、70 行（Setup 说明、广告管理费可选平台、服务边界表）已于 2026-09-02 同步修订，移除 ChatGPT 首投与管理承诺，标注"待 Connector 完成上线门槛后生效"。**ChatGPT Ads Connector 在仓库里仍完全不存在**，连暂停/改预算这种最基础的管理动作都做不到，仍不能出现在任何承诺列表里（不分自助档还是定制档）；在 Connector 从零建完并通过首投上线门槛之前，定制档销售**不得**按修订前的 playbook 原文签约 ChatGPT 相关条款。

**原因**：$499 的自助工具本身应该已经足够好用（不是"故意做残"来逼你升级）。定制档真正卖的不是"更强的工具"，是**跨 6 支柱的综合判断 + 对最终增长结果负责**——自助档是每根支柱各自转各自的小闭环，定制档是 ME 的人把 6 个小闭环拧成一个大闭环（这个月预算该往 SEO 还是广告倾斜、品牌调性怎么在各渠道保持一致）。这跟 IMPACT 强调"完整闭环"的逻辑一致，且现有 Enterprise FDE playbook 的实际内容（月度策略会、跨渠道预算分配、VI 一致性协调）本来就是在做这件事——但这条"内容不用重新设计"的结论**仅限于非广告的策略/协调服务**，广告部分（尤其 ChatGPT）仍受 Connector 完成门槛约束，两者不能混为一谈。

**待办**：
1. ~~`docs/registry/pricing-playbook-enterprise-fde.md` 第 17 行（Setup 里的"全部 3 个广告平台账号接管与首次投放"）与第 34 行（广告管理费可选平台）需要同步修订~~ **已完成（2026-09-02）**：该 playbook 第 17、34、70 行已同步修订，把 ChatGPT 从"可承诺"里移除并标注"待 Connector 完成后生效"，不再是上线阻塞项
2. 对外定价页文案需要把"定制档真正卖的是综合判断而不是更强工具"这句"卖点"说清楚，避免客户误以为"定制档只是功能更多的 $499"

---

## $199/$499 支柱额度量化（2026-09-02，把"更多/提升"这类模糊词换成具体数字）

| 支柱 | $39（已定）| $199 | $499 |
|---|---|---|---|
| 竞品 | 只读月度监控 | 每周 1 次，5-10 个具体竞品或整个细分行业 | 实时预警（重大动作发生后 1-2 天内），行业级全覆盖不设数量上限 |
| SEO 关键词 | 5-10 个种子词，月度快照 | 50-100 个关键词（含长尾+竞品缺口分析），周度刷新，内容 1 篇/月 | 200+ 关键词（按域名相关性过滤，非真无上限），日度监控，内容 4-8 篇/月 |
| 口碑 | GBP+1 个行业网络，月度只读 | AI 草拟回复覆盖当月所有新增评论（草稿，人工确认才发）| 4-5 星 24 小时内自动发出回复，1-3 星一律转人工，覆盖渠道扩到 2-3 个 |
| 社媒 | 2-3 条视频剪辑 + 10-15 张图/月 | 5-8 条视频剪辑 + 20-30 张图，2 周提前排期日历 | 12-20 条内容/月（每周 3-5 条）+ 40+ 张图 |

**口碑额度的判据来源**：PM 估计目标客户群平均每周收到 3-5 条 Google 评论（不同行业有差异，这是拍脑袋的量级估计，非实测数据），换算约 12-20 条/月，与 $499"24 小时自动回复"的设计量级吻合，暂不需要调整。

## 新增维度：团队席位（2026-09-02 PM 提出）

会员分档除了 6 支柱宽度/深度，还有一条**团队席位数**的独立轴：

| 档位 | 席位数 |
|---|---|
| 免费 | 1 |
| $39 | 1 |
| $199 | 3 |
| $499 | 10 |
| 定制 | 不限（按合同约定）|

## 订阅对比页设计稿（2026-09-02，v2 修订）

按完整矩阵 + 团队席位做了可视化对比页，供跟 jundong 碰版本用：https://claude.ai/code/artifact/9091afeb-bc4a-47fd-9a84-61aa16234e5e （本地源文件另存于会话 scratchpad，已发送给 PM）。页面上"研发中"标签对应本文档"能力现状核查"/"四个广告主仓库现状核查"/"跨档设计原则"里点出的工程缺口，不是漏标，是刻意保留给内部审阅用；对外发布前需要去掉这些标签或替换成真实上线状态。

**PM 2026-09-02 反馈修订 4 点**：
1. 第一列改按 6 支柱排序（SEO / AI 可见度(GEO) / 竞品 / 口碑 / 社媒 / 广告），SEO 与 AI 可见度相邻排在最前
2. 禁止"同左"这类偷懒描述——每一格都要按互联网 SaaS 产品页惯例完整写出，不依赖对照其他列
3. 加入 **Token（复用现有 MTC 概念）** 额度：每档每月赠送的 MTC 数量
4. **加购（Add-on）单独列在页面底部**，跟档位对比表分开

## Token（MTC）月度额度（2026-09-02 首版估算，待确认）

沿用现有 MTC 账本单位（`src/lib/mtc/`），按 $39 档已经算过的真实 compute 成本（~$1.5-3/月）折算出对应 MTC 数量，$199/$499 按同一逻辑外推，**这是第一版估算，不是精算结果**，PM 需要确认或调整：

| 档位 | 月度赠送 MTC |
|---|---|
| 免费 | 0（无支柱动作，纯基建）|
| $39 | 400 |
| $199 | 1,500 |
| $499 | 5,000 |
| 定制 | 不限（ME 按需配置，不计 MTC）|

**用完怎么办**：走现有 MTC 加购包（$99→1,000 / $249→2,800 / $599→7,500），不新造支付逻辑，只是把"一次性买"变成"月度赠送 + 随时加购"两条并存。

---

## 待定（下次接着问）

1. 产品负责人提到一句"之前有 issue 总结的挺好：199 是被看到，499 是花钱被看到"——**已搜遍仓库 issue/PR/commit，没找到原文**，需要产品负责人口述复原这句话的具体意思
2. **免费档"100 个名额"是一次性测试期定语，还是长期就限量制**——产品负责人说"看转化率再定，转化好可以补贴长期跑"，但这跟 #1274"永久免费不设上限"+ #1276 扫街批量预建站的玩法有冲突，**这条本质上要等真实转化数据才能拍板，不是现在能问出结果的问题**，先挂着，等免费档真上线跑出数据再回来对齐
3. **要不要在 `docs/specs/2026-06-05-mcp-phase2-product-design.md` 已拍板但尚未建成的通知基础设施（`notification_email`/`notification_log`/周报渲染模板，微信已拍板暂不做）上按会员档位加分层触发条件/频率**——见上方"获客渠道→客户画像"节：画像三明确不主动登录，画像二周活但登录频率低于画像一。层级判定已在上节完成（L1 平台基础设施，归位既有拍板设计，不登记候选，不得重新设计一套并行的通知渠道）；渠道选型也不是待定项（邮件复用已建成的 **Resend + `src/lib/email/sender.ts`**、不新增 SendGrid / 微信暂不做已拍板）；真正待定的只是分档推送的具体触发规则，且实施时要计入 `clients.notification_email` 字段 + 后台 UI、`notification_log` migration、复用 Resend 发信路径的推送渲染模板、周报 cron 路由/调度这条目前还没建的实施链路（见上节"渠道现状核查"）

## 已定 · 追加

- **删除注册送 500 MTC 老机制**——跟免费档"0 MTC"的新设计对齐，代码改动见 [PR #1339](https://github.com/bigbigraydeng-maker/magic-engine/pull/1339)（`fix/retire-signup-bonus-500mtc` 分支，独立于本文档所在分支，因为动的是生产代码不是文档，按"大任务"标记待复审，未自动合并）——2026-09-02
- **四个客户画像 + 反推日常功能优先级**（见上方"获客渠道→客户画像"节）——2026-09-02

- **视频功能默认 = 剪辑客户上传素材，不是 AI 从零生成**（原因、复用发现见上方"$39 档"节"跨档设计原则"）——2026-09-02 拍板
- **agentic 三轴模型修正**（宽度/深度/综合实现能力，agent 从 $39 起就在跑，不是某价位才解锁）——2026-09-02
- **$199/$499 六支柱矩阵定稿**（见上表）——2026-09-02，广告支柱笼子设计原则（只限预算不限功能）+ 四个广告主仓库现状核查一并定稿
- **定制档不重新设计，重新定义卖点**（见上节）——2026-09-02
- **$499 广告笼子 = 客户自己设的真实预算，不设 ME 定的新硬顶**；$199→$499 的差异是"判断更深"（更贵模型 + 更多数据信号），不是"笼子更大"——2026-09-02
- **五领域能力现状核查 + "诚实上线"修订**（AI可见度/SEO/社媒/CRM/邮件，见上方"能力现状核查"表）——2026-09-02
- **新增 4 功能**：llms.txt 自动生成（$39）、效果归因反馈（$199 起，补 IMPACT Check/Tune）、竞品广告库透视（竞品支柱）、本地同行批量发现（合并进竞品支柱定义）——2026-09-02
- **开放第三方 developer 共建**：已登记 candidate，判定为商业模式决策非技术分类，本次讨论不展开——2026-09-02
- **预算硬顶不是唯一安全阀**：动作授权、目标/创意回读、审批闸门、可回滚、紧急暂停按钮是独立于预算之外必须保留的护栏，对齐现有 `ad-draft.ts`/`ad-publisher.ts` 的"暂停创建→回读→人工批准→激活"模式——2026-09-02 补充（Claude Code Review 机器人三轮审阅）
- **四个广告主完整动作链缺口清单**（campaign/ad group/creative 创建、素材上传、投放后回读、激活验证、护栏模式）：**Meta 也未达首投可上线**——起草没有调用方（P21.J.M4）、素材上传未接（P21.J.M5）、`approveDraft` 激活前不重新回读、买家文案缺失只 warn 不阻断激活、**批准/否决不是原子认领**（`approveDraft`/`rejectDraft` 都是先读后无条件写，并发点击或否决已激活的草案会导致账本显示 `rejected` 但 Meta 广告仍在花钱，`rejectDraft` 甚至不检查当前状态，见 `docs/ROADMAP.md:185-186`）；这五条补齐前 $199/$499 定价页对任何广告主都不能承诺"自助投放 Agent"或"从零创建广告系列"，Google/TikTok 现在只能承诺广告管理；**ChatGPT Ads 不进任何承诺列表**——仓库里连管理用的 Connector 都没有——2026-09-02 三轮审阅修正（Meta 接线/安全缺口纳入首投门槛）、2026-09-02 五轮审阅补记批准/否决原子认领缺口
- **$39 AI 可见度频率统一为月度**（基础额度包），周度提频只在"挖爽感加码版"里，不是基础档承诺——2026-09-02 补充
- **$39 视频剪辑额度定为 2-3 条/月**（计入基础额度包，成本约 worker 渲染+存储+传输，区别于加码版额外送的 1 条 AI 生成视频）——2026-09-02 补充；**这条剪辑链路目前只有 CLI proof 脚本（`renderWalkTalkProof`），没有接入生产 worker**——`content-factory/[postId]/video/route.ts` 只接收人工已剪好的成片落库，产品化剪辑管道是 GO BUILD 缺口，不是"基本不用新建"——2026-09-02 三轮审阅修正
- **$39 档基础额度包毛利改口径**：92-96% 只是 compute-only contribution margin，加上 Stripe 手续费（$1.431/笔）后真实毛利约 88.6%-92.5%，加码版约 83.5%-88.6%，且都未计入 worker/存储/传输/客服/基础设施——2026-09-02 三轮审阅修正
- **"自主执行"边界澄清**：$499 与定制档的"自主"都只指 AI 自主判断+起草广告草稿，不豁免"暂停创建→回读→人工批准→激活"护栏——每次激活仍需人工回读确认，两档在这一点上完全一致，差异只在配置方是谁——2026-09-02 补充
- **月度累计预算闸列为独立上线阻塞项**：`$1,000/月硬顶`目前无工程实现——`ad-draft.ts` 只查单条草案的日预算/时长上限，`client_automation_policies.spend_cap_per_period_usd` 明确标注 `RESERVED · NOT ENFORCED`；跨 campaign/平台的累计核算、月份窗口语义、币种语义三项须在 GO BUILD 前补齐，且与四个广告主的接线缺口分开验收——2026-09-02 四轮审阅新增
- **定制档"沿用 playbook"收窄为不含 ChatGPT**：Enterprise FDE playbook 原文仍把 ChatGPT 写进 Setup 首投与广告管理费可选平台，与"ChatGPT Ads 不进任何承诺列表"的结论矛盾；已同步修订 `docs/registry/pricing-playbook-enterprise-fde.md`（Setup 说明、广告管理费可选平台、服务边界表三处），移除 ChatGPT 承诺，标注待 Connector 补齐后生效——2026-09-02 四轮审阅修正

---

## 历史

- 2026-09-01 · 建档，吸收多轮 PM 对话（免费档基建定位、CRM Connector 原则、$39 麻雀虽小闭环原则、支柱进场顺序、$39 成本测算与爽感加码）
- 2026-09-02 · skill 加别名"张居正"；$39 视频改为剪辑客户素材而非 AI 生成；修正 agentic 三轴模型（宽度/深度/综合实现能力）；$199/$499 六支柱矩阵定稿；广告笼子设计原则（只限预算不限功能）+ 四个广告主仓库现状核查；定制档卖点重新定义
- 2026-09-02 · AI可见度/SEO/社媒/CRM/邮件五领域能力现状核查 + 按"诚实上线"二次修订矩阵；新增 llms.txt/效果归因反馈/竞品广告库透视 3 功能；"开放第三方 developer 共建"登记候选，本次不展开
- 2026-09-02 · Claude Code Review 机器人一轮审阅修正：广告护栏补充"预算不是唯一安全阀"+ 独立护栏清单；四个广告主动作链缺口从摘要改为逐项列出并收窄定价承诺；$39 AI 可见度频率统一为月度（周度仅加码版）；$39 视频剪辑额度明确为 2-3 条/月并同步更新基础额度包成本测算
- 2026-09-02 · 二轮审阅修正：ChatGPT Ads 从"可承诺广告管理"改为不进任何承诺列表（仓库里连管理 Connector 都没有）；澄清"自主执行"只指判断+起草，不豁免"人工批准"激活护栏，消解与 $499/定制档描述的表面矛盾
- 2026-09-02 · 三轮审阅修正：Meta 从"成熟"改为"代码具备但接线未完整"，起草无调用方/素材上传未接/批准前不重新回读/买家文案缺失不阻断激活四条纳入首投门槛，任何广告主的"自助投放 Agent"承诺都要等这些缺口补齐；"边走边录"自动剪辑澄清为仅有 CLI proof 脚本、未接入生产 worker，$39 视频剪辑链路补记为 GO BUILD 缺口；$39 基础额度包毛利改口径为 compute-only contribution margin，补记加上 Stripe 手续费后的真实毛利区间
- 2026-09-02 · 四轮审阅修正（Codex review round 3）：新增"月度累计预算闸"为独立于四个广告主之外的上线阻塞项（跨 campaign/平台累计核算、月份窗口、币种语义均未实现）；定制档"沿用 Enterprise FDE playbook"收窄为不含 ChatGPT，并同步修订该 playbook 移除 ChatGPT 首投/管理承诺
- 2026-09-02 · 五轮审阅修正：Meta 首投门槛从四条缺口补齐为五条——新增"批准/否决不是原子认领"（`approveDraft`/`rejectDraft` 都是先读状态再无条件写，`rejectDraft` 连当前状态都不检查），两人同时点或有人否决已 `active` 的草案会导致账本显示 `rejected` 但 Meta 广告仍在花钱，与 `docs/ROADMAP.md:185-186` 已标注的"升级为首投前置"对齐
- 2026-09-02 · $199/$499 支柱额度全部量化（关键词数/评论响应时效/内容条数等具体数字，来源含 PM 口径估计）；新增团队席位轴（1/1/3/10/不限）；产出订阅对比页可视化设计稿（Artifact），供跟 jundong 碰版本
- 2026-09-02 · 删除注册送 500 MTC 老机制（独立 PR #1339，待复审）；梳理获客渠道倒推出 4 个客户画像，反推出"主动通知/摘要推送"应提升为跨支柱基础设施能力的洞察
- 2026-09-03 · 六轮审阅修正（Codex PR #1341）：收窄"画像二、三根本不会主动打开仪表盘"为只对画像三成立（画像二上表定义是"周活不是日活"，非零登录），除非有真实使用数据再外推；补记通知基础设施在 `docs/specs/2026-06-05-mcp-phase2-product-design.md` 已有拍板（`notification_email`/`notification_log`/周报 cron/微信暂不做），要求下次续聊前先跑 `me-platform-tier-gate` 判定复用/修订/废弃，不得重新设计一套并行通知渠道
- 2026-09-03 · 七轮审阅修正（Codex PR #1341 round 2）：不再把层级判定推迟到"下次续聊前"，本文内当场跑完 `me-platform-tier-gate` Inline 判定并给出结论（L1 平台基础设施，归位既有拍板，不登记候选）；同时修正把设计阶段的推送组件误写成"现有能力"的问题——SendGrid 封装、`src/lib/notifications/email.ts`、`notification_log` 表、`weekly-brief` cron 目前均未建成，仓库里没有 `src/lib/notifications/` 目录也没有 `weekly-brief` cron，明确标注这些是"已拍板但未落地"，实施时要计入完整建设工作量
- 2026-09-03 · 八轮审阅修正（Codex PR #1341 round 3）：上一轮把"SendGrid 未建成"当结论，但没有先按当前仓库状态重新跑 Reuse 判定——`docs/STATE.md:195` 早把 **Resend** 登记为全部事务邮件供应商、`docs/ENV.md:145` 已登记 `RESEND_API_KEY`、`src/lib/email/sender.ts` 已提供统一发信封装，且 `src/app/api/cron/weekly-seo-report/route.ts:38-60` 已用这套 Resend 链路每周给客户发周报；改口为邮件发信能力**复用已建成的 Resend**，不新增 SendGrid SDK / 不引入第二套邮件供应商，`admin-key-expiry/route.ts:5-6` 的"SendGrid not yet built"注释是没跟着供应商切换更新的过期文案，不代表仓库现状；剩余待建的只收窄为 `clients.notification_email` 字段 + 后台 UI、`notification_log` 表 migration、按会员档位的触发规则、复用 Resend 发信路径的渲染模板/cron 调度
