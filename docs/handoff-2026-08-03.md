# 新窗口交接提示词（2026-08-03）

复制下面整段发给新窗口即可。

---

接手 Magic Engine 的 DAPE 底座工作。先读这两份，别重新调查：

- `docs/dape-health-2026-08-03.md` —— 今天查实的 DAPE 体检结论与证据
- `docs/FILING.md` —— 东西该放哪（合同/素材/文档）

**工作目录**：`.claude/worktrees/seo-watchdog-fixes`（git worktree，**绝不要 cd 到主仓库**，主仓库停在别的窗口的分支上）
**分支**：`feat/viral-structure-into-factory`，**已领先 main 24 个提交但还没开 PR** —— 先确认这些改动要不要合，别在上面继续堆。

---

## 今天已经做完的（别重做）

1. **自动任务健康检查** —— 40 个 cron 的「该跑没跑」检测，接进今日待办。
   `src/lib/cron/{schedule,registry,health}.ts` + 与 render.yaml 的对账测试
2. **目标数字口径检查** —— `src/lib/strategy/baseline-audit.ts`，接进待办
3. **深度诊断周更** —— `src/app/api/cron/diagnostic-weekly` + `src/lib/diagnostic/scheduled-run.ts`，
   已加 render.yaml 与 CRON_REGISTRY。周一 08:00 UTC 跑，结果进待办
4. **视频工厂**：客户素材接进底图池、爆款结构定节奏、服务承诺红线、楼盘级素材隔离

---

## 待办（按优先级）

### 1. P 段（处方）自动化 —— DAPE 还差这一环
新目标最近 2026-06-19（6 周前）、新战役 2026-06-08（8 周前）。
A 段已经通了（周更诊断），但诊断结论没有自动变成新的目标/战役。
**接之前先想清楚：谁消费？** 今天所有毛病都是同一个形状 ——
「有数据没人看」。没有消费方就别建。

### 2. 需要 PM 动手的三件（已在今日待办里，可催）
- **AI 出片余额用完**（muapi.ai/topup）—— 充值后重跑 Oztop 那条片子
- **3 个从没跑过的 cron**：factory-order-scheduler / job-boards-weekly / viral-discovery-weekly
  多半是 Render 里没关联 `me-shared-cron-secret`
- **CTS「团报名 342」**：那是 GA4 key events 不是报名（1074 访问 342 转化 = 32%，不可能）。
  需 PM 去 GA4 看哪些事件被标成了 key event

### 3. 视频工厂剩余
- Oztop 那条片子重跑（余额到位后）。上次成品 1080x1920 / 15 秒 / $0.45
- 出片工人**跑在 PM 的 Mac 上**，不是服务器 —— 工厂目前无法自己出片，这是最大结构缺口

---

## PM 今天的拍板（不要推翻）

- 客户 = **中介**（Roman Hu），楼盘是中介名下的项目，**发票直接开给开发商**。
  例外：开发商自己要做品牌 branding，才是独立客户
- 「楼盘」「房子」「行程单」是**行业专属工具**，按行业门控，**默认隐藏**
- 诊断结果**进今日待办逐条看**
- 客户自己实拍的素材**坚决不可共用**给同行

---

## 今天踩过的坑（重复踩会很贵）

1. **写 `.select()` 前必须先查真实列名。** 踩过三次：`quality_score` 在 jsonb 里不是列、
   `failure_reason` 实际叫 `reject_reason`、目标页路径是 `/clients/<客户id>/goal/<目标id>`。
   查询报错会被 catch 吞掉 → 功能静默失效 → **单测用自编数据形状照样全绿**
2. **登录类站点的链接机器验不了。** GSC 正确链接和错误链接对 curl 都返回 200，
   404 是登录后前端画的。所以那类一律给稳定入口 + 文字步骤，不给深链
3. **误报一旦成常态，告警就废了。** 阈值宁可宽，宁可漏报
4. **「看不见」不等于「没问题」。** 代码里不写运行记录的任务要单独报，不能算健康
5. **错的数字比没有数字更危险。** 我今天差点据一个反向数字建议 PM 砍掉正在见效的动作

---

## 会话结束前必做（CLAUDE.md §9）

未完成任务回写 `docs/ROADMAP.md`；已上线功能写 `docs/history/CHANGELOG.md`；
实际操作过的客户各写一条 `fde_work_logs`。
