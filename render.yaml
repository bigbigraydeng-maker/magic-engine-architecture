services:

  # ⚠️ 本 blueprint 不定义 web 服务。
  #
  # 对外的生产 Web 服务是 Render 上的 `crazycontent`(srv-d79s6d95pdvs73boptcg),
  # 它持有 app.magicengine.com.au / magicengine.com.au / magicengine.cloud 三个域名、
  # 55 个环境变量(含 CRON_SECRET 与全部 API key),从 bigbigraydeng-maker/magic-engine
  # 的 main 分支自动部署,归属另一个 blueprint。
  #
  # 这里原本还定义了一个同仓同分支的 web 服务 `magic-engine` —— 它没有域名、
  # 环境变量只配了 16 个(缺 CRON_SECRET 和所有 API key),从未对外服务过,
  # 只是每次 push 都构建一次、失败一次、发一封告警邮件。已于 2026-07-25 移除。
  # 下面的 cron 全部 curl app.magicengine.com.au,即打到 crazycontent,与此无关。

  # NOTE: Voice Agent realtime link runs IN-PROCESS inside the web service (MVP mode) —
  # the webhook opens the OpenAI realtime WebSocket directly, so NO separate worker
  # service is needed. To scale it out later, deploy scripts/voice/realtime-worker.ts as
  # a Render private service and set REALTIME_WORKER_URL on the web service to its
  # internal URL (see docs/voice-agent/RUNBOOK.md).

  # Vision Asset Intelligence analyzer — every 10 minutes (降频于 2026-07-21, commit b9f8189)
  # Picks up pending client_assets, runs GPT-4o-mini Vision, writes scores.
  # Reference: docs/history/CHANGELOG.md (P21.B.3)
  # healthchecks.io: vision-analyzer (1b1cd19b-583a-41ac-baaa-c9415241d5e8)
  # 2026-08-03 降频 */10 → */30：24h 内 144 次运行全部零产出（client_assets
  # 无 pending 行，最新素材停在 2026-06-10）。非交互式批处理——客户传完素材就走，
  # 由 FDE 事后审阅，晚 30 分钟开始分析无感知。审计见 docs/audits/supabase-request-audit.md §8 P0-1
  # 2026-08-16 [OPS] 降频 */30 → 每小时第 50 分：30 天仍全程零产出（工厂未开工），
  # 且本任务本来就是非交互批处理，没人在前台等它。回滚：schedule 改回 "*/30 * * * *"。
  - type: cron
    name: vision-analyzer
    env: node
    schedule: "50 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 120 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/vision-analyzer \
        && curl -fsS "https://hc-ping.com/1b1cd19b-583a-41ac-baaa-c9415241d5e8"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Poll visual generation jobs every 10 minutes
  #
  # ⚠️ 2026-08-03 审计：本任务 24h 内 144 次运行同样零产出，但**刻意不降频**。
  # 它是用户点了「生成视频」之后在等的完成检查（ReelsStudio 的生成按钮会写
  # reels_drafts.status='video_generating'，另有 3 个接口写 visual_assets
  # generation_status='generating'），降频 = 让人干等。空跑只说明当下没人在生成，
  # 不代表可以放慢。别顺手把它跟隔壁三个一起降了。
  # healthchecks.io: poll-visual-jobs (0dd2b641-499f-4f2f-8b73-73085050a5f0)
  #
  # 🔴 2026-08-16 [OPS] 降频 */10 → 每小时第 15 分 —— **这一条直接推翻了上面那段
  #    2026-08-03 的「刻意不降频」结论**，由 PM 在省钱盘点里显式点名要降，不是顺手带的。
  #    代价说清楚：用户在 ReelsStudio 点完「生成视频」后，成片最坏要等 60 分钟才在界面
  #    上变成完成态（此前最坏 10 分钟）。生成本身照跑，慢的只是「什么时候被看见」。
  #    工厂开工 / 有人开始天天用生成按钮时，这一条要第一个改回去。
  #    回滚：schedule 改回 "*/10 * * * *"。
  - type: cron
    name: poll-visual-jobs
    env: node
    schedule: "15 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/poll-visual-jobs \
        && curl -fsS "https://hc-ping.com/0dd2b641-499f-4f2f-8b73-73085050a5f0"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Site Audit job maintenance (cleanup, watchdog, recovery)
  # Runs daily at 2 AM UTC (~2 PM NZST)
  # Functionality:
  #   - Cleanup: Delete jobs completed > 30 days ago
  #   - Watchdog: Detect and log failed jobs (status='failed' with error_message)
  #   - Recovery: Resume pending jobs stuck > 1 hour
  # Reference: ROADMAP.md P8.0.5.6
  # healthchecks.io: site-audit-cron (8451084d-23c0-44b0-af00-8e29ff80fcdc)
  - type: cron
    name: site-audit-cron
    env: node
    schedule: "0 2 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 120 -X POST -H "x-cron-secret: $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/site-audit-jobs \
        && curl -fsS "https://hc-ping.com/8451084d-23c0-44b0-af00-8e29ff80fcdc"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Flywheel attribution job — every 6 hours
  # Computes baseline/after/verdict for every flywheel_action with an expected_metric.
  # Reference: ROADMAP.md P12.A.9
  # healthchecks.io: attribution-cron (87926446-35cf-4e5b-a868-bb784c27ee5f)
  - type: cron
    name: attribution-cron
    env: node
    schedule: "0 */6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/attribution \
        && curl -fsS "https://hc-ping.com/87926446-35cf-4e5b-a868-bb784c27ee5f"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Zhangqian discovery sweeper — every 5 minutes
  # Marks stuck pending/running discovery jobs (>10 min) as failed.
  # The status route's stale check is reactive (UI-poll driven), so jobs
  # whose UI was closed mid-run would otherwise sit running forever.
  # healthchecks.io: zhangqian-sweeper (071910fe-b9b7-435d-8b72-f141818eb03d)
  # 2026-08-03 降频 */15 → */30：24h 内 96 次运行全部零产出。纯超时兜底，从不干活。
  # 只降到 30 分钟而非每小时：超时阈值是 12 分钟（2026-08-25 从 6 分钟上调，见
  # src/lib/zhangqian/agent.ts 的 call budget 段），每小时跑会让真卡住的任务在
  # 界面上「运行中」挂最多 72 分钟，FDE 会误判系统坏了；30 分钟档最坏 42 分钟。
  # ⚠️ 2026-08-16 [OPS] 降频 */30 → 每小时第 35 分 —— 上面那句「不降到每小时」被 PM
  #    在省钱盘点里显式推翻。代价照旧成立：真卡住的发现任务在界面上最坏挂 72 分钟。
  #    工厂 / 发现线开工后若 FDE 反馈「看着像坏了」，第一步就是把这条改回去。
  #    回滚：schedule 改回 "*/30 * * * *"。
  - type: cron
    name: zhangqian-sweeper
    env: node
    schedule: "35 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/zhangqian-sweeper \
        && curl -fsS "https://hc-ping.com/071910fe-b9b7-435d-8b72-f141818eb03d"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Prospecting auto-sweep — every 10 minutes (Phase 35)
  # Advances the outbound conveyor one bounded step per fire:
  #   audit -> analyze -> draft -> discover next industry x NZ-city seed.
  # No-ops unless PROSPECTING_SWEEP_ENABLED=true on the web service (this cron only
  # curls; a flag set here would never be read). Ships dark; PM turns spend on.
  # Sending stays human — drafts land in the review queue only.
  - type: cron
    name: prospecting-sweep
    env: node
    schedule: "0 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/prospecting-sweep
    envVars:
      - key: CRON_SECRET
        sync: false

  # Job-signal discovery — weekly (Mon 02:00 UTC ≈ Mon 2pm/3pm NZST).
  # Harvests NZ marketing-role postings (Seek), resolves each hiring company via
  # Places, inserts new ones as discovered(job_board). The prospecting-sweep then
  # audits/analyses/drafts them. No-ops unless JOB_SIGNAL_INGEST_ENABLED=true on
  # the web service (ships dark; PM turns it on). Sending stays human downstream.
  - type: cron
    name: job-boards-weekly
    env: node
    schedule: "0 2 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/job-boards-weekly
    # 2026-08-16 [OPS] sync:false → fromGroup：密钥自动挂上，不用人去后台 link。
    # ⚠️ 这条的「30 天没跑成」根因**不是**密钥 —— 是主开关 JOB_SIGNAL_INGEST_ENABLED
    #    没开（route 在开关那里直接 return，走不到写运行记录那一步）。换 fromGroup 只是
    #    顺手排除掉第二个可能原因；真要让它干活，得 PM 决定开外呼线（花 Places 的钱 +
    #    开始碰真实 NZ 公司），不是这个 PR 能定的。
    envVars:
      - fromGroup: me-shared-cron-secret

  # Blog generation sweeper — every 5 minutes
  # Marks blog_posts stuck at status='generating' (>10 min) as failed.
  # Background generation in /api/clients/[id]/blog is fire-and-forget;
  # if Render restarts mid-run, rows would otherwise sit generating forever.
  # Reference: ROADMAP.md P14.C.1
  # healthchecks.io: blog-stuck-generating-sweeper (180d3e65-1910-4f8d-a778-409571d5fb46)
  # 2026-08-03 降频 */15 → */30：24h 内 96 次运行全部零产出（blog_posts 无 generating 行）。
  # 纯超时兜底，从不干活。同 zhangqian-sweeper，只降到 30 分钟：超时阈值 10 分钟，
  # 每小时跑会让卡住的文章在界面上「生成中」挂最多 70 分钟；30 分钟档最坏 40 分钟。
  # ⚠️ 2026-08-16 [OPS] 降频 */30 → 每小时第 45 分 —— 上面那句「不降到每小时」被 PM
  #    在省钱盘点里显式推翻。代价照旧成立：卡住的文章最坏「生成中」挂 70 分钟。
  #    回滚：schedule 改回 "*/30 * * * *"。
  - type: cron
    name: blog-stuck-generating-sweeper
    env: node
    schedule: "45 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/blog-stuck-generating \
        && curl -fsS "https://hc-ping.com/180d3e65-1910-4f8d-a778-409571d5fb46"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Marks tailor_made_jobs stuck at status='queued'/'running' (>10 min) as failed.
  # Same pattern as blog-stuck-generating-sweeper above: import/extract 建完任务行
  # 立刻回 202，AI 生成走 fire-and-forget；Render 部署重启会把半路的任务永远
  # 晾在 queued/running。getJob()/createOrReuseJob() 已经有惰性自愈（被轮询或
  # 被同一份行程再次建任务时会自动纠正），这个 cron 只兜底"从此没人再碰"的情况。
  # 见 CTS 2027 China Panorama 报障后续修复（tailor_made_jobs 表新增）。
  # 密钥用 fromGroup 自动挂，不要改回 `sync: false`——那条路径不会真的给新服务
  # 灌值，line ~980 那条 cron 的注释已经记过一次这个坑（连续 401 且没人发现）。
  # ⚠️ 没有配 healthchecks.io ping —— 没有权限新建 check，PM 有空的话在
  #    healthchecks.io 建一个再把 hc-ping 行加回来（参照上面 blog-stuck-generating 的写法）。
  - type: cron
    name: tailor-made-jobs-sweeper
    env: node
    schedule: "*/30 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/tailor-made-jobs-sweeper
    envVars:
      - fromGroup: me-shared-cron-secret

  # Weekly AI Visibility Tracker — every Monday at 1am UTC (~1pm NZST)
  # Processes all clients with enabled queries; writes runs + weekly snapshot.
  # --max-time 30: route returns 202 immediately (fire-and-forget), so 30s is
  # plenty. Avoids Cloudflare 524 timeout that killed previous sync runs.
  # Reference: ROADMAP.md P7.4.13
  # healthchecks.io: ai-tracker-weekly (c4f101fe-c2cf-47f9-807f-08ea09d43934)
  # Schedule moved from Mon 01:00 UTC to Sun 18:00 UTC = Mon 06:00 NZST (PM decision 2026-09-16: NZ morning window).
  - type: cron
    name: ai-tracker-weekly
    env: node
    schedule: "0 18 * * 0"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 30 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/ai-tracker-weekly \
        && curl -fsS "https://hc-ping.com/c4f101fe-c2cf-47f9-807f-08ea09d43934"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily Industry AI Visibility Archive — every day at 16:30 UTC (~4:30am NZST)
  # Runs all 10+ industry questions × 3 platforms (ChatGPT / Google AI Overview /
  # Google SERP), upserts one snapshot per (question, platform, collected_date).
  # PM decision 2026-06-04: daily (not weekly) — only daily granularity captures
  # brand churn within the week. Cost ~$0.06/day for 10 seed questions.
  # Migration 20260622000003 enabled daily UNIQUE constraint.
  # Schedule moved from 02:30 UTC to 16:30 UTC (PM decision 2026-09-16: run in NZ morning window so data is ready on wake-up).
  # healthchecks.io: industry-ai-visibility-daily (1400fe83-cc62-4634-9f7e-a2345553beee)
  - type: cron
    name: industry-ai-visibility-daily
    env: node
    schedule: "30 16 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 600 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/ai-visibility-weekly \
        && curl -fsS "https://hc-ping.com/1400fe83-cc62-4634-9f7e-a2345553beee"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Phase 31 A2.1-γ — Goal current_value refresh
  # Daily 17:30 UTC (~5:30am NZST), runs AFTER industry-ai-visibility-daily (16:30 UTC)
  # so ai_visibility_score Goals read today's snapshots (not yesterday's).
  # Spec: docs/superpowers/specs/2026-06-04-a21-stepc-design-v3.md (v3.1)
  # Schedule moved from 03:00 UTC to 17:30 UTC (PM decision 2026-09-16: NZ morning window).
  # healthchecks.io: goal-current-value-refresh (300bb7f9-8a16-4a72-b9eb-a924dc76efa8)
  - type: cron
    name: goal-current-value-refresh
    env: node
    schedule: "30 17 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/goal-current-value-refresh \
        && curl -fsS "https://hc-ping.com/300bb7f9-8a16-4a72-b9eb-a924dc76efa8"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly SEO keyword snapshots — every Monday at 2am UTC (~2pm NZST)
  # Stores DataForSEO ranked keyword rows for SEO Intelligence trends.
  # Reference: ROADMAP.md P12.I.8
  # healthchecks.io: keyword-snapshots-weekly (db156874-3c69-4b5a-b534-a6bbea63a467)
  - type: cron
    name: keyword-snapshots-weekly
    env: node
    schedule: "0 2 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/keyword-snapshots-weekly \
        && curl -fsS "https://hc-ping.com/db156874-3c69-4b5a-b534-a6bbea63a467"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly reputation snapshots — every Monday at 3:30am UTC (~3:30pm NZST)
  # GBP / Tripadvisor rating + review count + incremental reviews for active
  # clients with reputation identities configured (DataForSEO 计划 阶段 2).
  # ⚠️ 新 cron 上线后必须在 Render 后台 link "me-shared-cron-secret" env group，
  #    否则 CRON_SECRET 为空 → 每周 401 静默失败（daily-cron-digest 事故教训）。
  - type: cron
    name: reputation-snapshots-weekly
    env: node
    schedule: "30 3 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/reputation-snapshots-weekly
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily social engagement pullback — every day at 4am UTC
  # Pulls Publer post likes/comments/shares into flywheel_metrics.
  # Reference: ROADMAP.md P12.C.3
  # healthchecks.io: social-engagement-pullback (01c469cc-4e77-4e88-bba4-7f17cbec91bc)
  - type: cron
    name: social-engagement-pullback
    env: node
    schedule: "0 4 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/social-engagement-pullback \
        && curl -fsS "https://hc-ping.com/01c469cc-4e77-4e88-bba4-7f17cbec91bc"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily Winner Reel Auto-Sync — every day at 15:00 UTC (= 03:00 NZST, NZST=UTC+12)
  # Phase 34.A: scans a client's FB Page organic Reels, promotes winners into the
  # ThruPlay pool (new Ads default PAUSED, FDE approves). Migrated off GitHub Actions:
  # GHA scheduled workflows are best-effort and the 2026-07-11 15:00 UTC run never fired.
  # Render Cron Scheduler is a dedicated scheduler and fires reliably.
  # Reference: docs/sops/winner-reel-auto-sync.md · PR #542/#545
  # TODO(healthchecks): create a healthchecks.io monitor + append its hc-ping URL as a
  #   dead-man switch (emails if a run is missed). Left off until a UUID is provisioned;
  #   the cts-meta-daily-selfcheck scheduled task already flags a missed run in the interim.
  - type: cron
    name: winner-reel-sync-daily
    env: node
    schedule: "0 15 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/winner-reel-sync-daily
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily Google data pullback — every day at 3am UTC (~3pm NZST)
  # Syncs GSC + GA4 snapshots for all clients with connected Google connectors.
  # Reference: ROADMAP.md P17.A.4
  # Schedule moved from 03:00 UTC to 17:00 UTC (PM decision 2026-09-16: NZ morning window; runs 30 min after industry-ai-visibility-daily).
  # healthchecks.io: google-data-pullback-daily (12f3c5c4-4524-48b8-92c5-72fd2b775d01)
  - type: cron
    name: google-data-pullback-daily
    env: node
    schedule: "0 17 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/google-data-pullback-daily \
        && curl -fsS "https://hc-ping.com/12f3c5c4-4524-48b8-92c5-72fd2b775d01"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly 诸葛亮 recalculation — every Monday at 3am UTC
  # Re-runs priority analysis for all clients with confirmed discovery,
  # writes fresh recommendations to execution_items kanban.
  # Reference: ROADMAP.md P24.B
  # healthchecks.io: zhuge-weekly-recalculate (a2c4d128-e09c-41eb-843e-98c9a0afecba)
  - type: cron
    name: zhuge-weekly-recalculate
    env: node
    schedule: "0 3 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/zhuge-recalculate \
        && curl -fsS "https://hc-ping.com/a2c4d128-e09c-41eb-843e-98c9a0afecba"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Viral reference analyzer worker — every 2 minutes
  # Throttled queue drainer: picks up to 8 pending videos, runs Gemini analysis
  # sequentially with 5s delay between calls. Without this, viral-discovery would
  # burst 150+ concurrent Gemini calls and hit paid-tier TPM cap (1M tok/min).
  # healthchecks.io: viral-analyzer-worker (55891cbc-cfd5-4c58-b28b-f06206fade5a)
  - type: cron
    name: viral-analyzer-worker
    env: node
    schedule: "*/10 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/viral-analyzer-worker \
        && curl -fsS "https://hc-ping.com/55891cbc-cfd5-4c58-b28b-f06206fade5a"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily viral YouTube discovery — every day at 0am UTC (~12pm NZST)
  # 8 industries × 6 keyword groups × limit 20 = up to ~960 videos/day.
  # Auto-deduplicates, inserts new rows, kicks off Gemini analysis.
  # Quota cost ~5,760 units/day (free tier = 10,000/day).
  # healthchecks.io: viral-discovery-weekly (e5c6cbe8-e6cc-457f-bb3f-538971dafd46)
  - type: cron
    name: viral-discovery-weekly
    env: node
    schedule: "0 0 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/viral-discovery-weekly \
        && curl -fsS "https://hc-ping.com/e5c6cbe8-e6cc-457f-bb3f-538971dafd46"
    envVars:
      - key: CRON_SECRET
        sync: false

  # 内容工厂 · 选题进料 — 每天 UTC 22:00 (~10am NZST) 抓爆款+按人设改写成候选。
  # cron 每天打；路由内部按每个客户的 topic_intake.cadence 过滤(daily 每天/weekly 只周一/off 跳)。
  # ⚠️ 部署后需在 Render 手动 link me-shared-cron-secret env group，否则每天静默 401。
  #    建议再配一个 healthchecks.io ping(&& curl hc-ping.com/<uuid>)防"没跑"不报警。
  - type: cron
    name: content-factory-intake
    env: node
    schedule: "0 22 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/content-factory-intake
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily anomaly detector — every day at 5am UTC (~5pm NZST / 3pm AEST)
  # Two-step pipeline:
  #   Step 1: AnomalyDetectorJob — scans flywheel_metrics, writes anomaly_signals
  #   Step 2: 诸葛亮 Proactive — reads fresh signals, creates proactive execution tasks
  # Reference: ROADMAP.md § Phase 22.D.3
  # healthchecks.io: anomaly-detector-daily (ff78126a-25e2-4a50-abcb-edbcb1dfaec9)
  # 深度诊断周更 —— 补上 DAPE 的 A 段(分析)。
  # 此前深度诊断**没有任何定时任务**:只能人工点按钮或新客户建档时跑一次,
  # 最近一次 2026-07-20。于是执行段天天在跑,执行的却是两个月前的结论。
  # 🔴 花钱路径:六维度全量采集 + AI 综合,比规则引擎型的 anomaly-detector 贵一个量级,
  #    所以周更不日更;且只跑「有域名 + 填了行业的 active 客户」—— 内部账号
  #    (Magic Engine / Magic Lab / Magic Lab Class) 三个都有域名,光看域名拦不住,
  #    真实判据是有没有填行业。同客户 5 天冷却,防手工刚跑完 cron 又跑一遍。
  # 周一 08:00 UTC ≈ 周一 20:00 NZST,跑完赶上周二早上的待办。
  - type: cron
    name: diagnostic-weekly
    env: node
    schedule: "0 8 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 800 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/diagnostic-weekly
    envVars:
      - key: CRON_SECRET
        sync: false

  # 🔴 效果回流 —— DAPE 这个圈能不能合拢，全靠下面这两条。
  #
  # 2026-08-04 查出来：这两个接口**代码早就写好了，但 render.yaml 里没有、
  # 登记表里也没有 —— 从来没有任何东西调用过它们**。后果是
  # `prescription_outcomes` 从建库到今天**一条记录都没有**：
  # 「我们给客户出的方案到底有没有用」这个问题，系统一次都没回答过。
  # 没有这一环，下一轮出方案时 AI 不知道上一轮哪条管用、哪条白干。
  # 而且因为它们不在登记表里，连「没跑」的告警都不会报 —— 又是「看不见」。

  # 处方效果实测回填 —— 每天看有没有处方到了批准后 30/60/90 天的节点，
  # 到了就拉真实指标写进 prescription_outcomes。日更但只在节点当天真干活。
  - type: cron
    name: kpi-backfill
    env: node
    schedule: "20 6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/kpi-backfill
    envVars:
      - key: CRON_SECRET
        sync: false

  # 行业基准累积 —— 把上面攒下来的实测值按行业/规模/市场分组算 P50/P75/P90。
  # 周更即可；样本不够时本来就不该写，会在运行记录里写明「等实测结果攒够」。
  # ⚠️ 这个 route 只实现了 GET，别加 -X POST（会 405）。
  - type: cron
    name: benchmark-accumulator
    env: node
    schedule: "40 7 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/benchmark-accumulator
    # 2026-08-16 [OPS] sync:false → fromGroup。这条 route 在鉴权之后立刻写运行记录、
    # 中间没有任何开关，所以「30 天零运行记录」只可能是压根没跑成 —— 401 静默失败。
    # 参照 mailchimp-activity-daily 的写法，密钥由环境变量组自动挂上。
    envVars:
      - fromGroup: me-shared-cron-secret

  # 发布通道自动重测 —— 让临时故障自己好。
  # 🔴 起因:Oztop 的 WordPress 通道 2026-06-19 被客户主机商的安全防护拦了一次,
  #    标成 error 之后 **47 天没有任何东西重测过**(全仓没有 cron 碰 cms_connections),
  #    于是一次几分钟的拦截变成永久停止发布,而周更 cron 还在照常写文章。
  # 只测 status='error' 的行,不碰正常连接。同一条最短 20 小时才再测一次。
  # ⚠️ 这个 route 只实现了 GET,别加 -X POST(会 405)。
  - type: cron
    name: cms-connection-retest
    env: node
    schedule: "10 6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/cms-connection-retest
    envVars:
      - key: CRON_SECRET
        sync: false

  # DAPE P 段(处方)—— 吃周一体检的结果开方,并自动落地成执行看板上的动作。
  # 🔴 花钱路径:每个客户打一次 AI。同客户 6 天冷却。
  #    刻意跟体检分成两个任务:混在一起挂了分不清是没采到数据还是没生成方案。
  # 周二 08:00 UTC,排在体检后一天。
  - type: cron
    name: prescription-weekly
    env: node
    schedule: "0 8 * * 2"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 800 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/prescription-weekly
    envVars:
      - key: CRON_SECRET
        sync: false

  # DAPE E 段(执行)—— 把看板上排好的动作真正跑掉,产物只落草稿。
  # 🔴 起因:2026-08-05 实测 execution_items 里 281 件待办,最后一件「完成」在 7-21。
  #    判定用的两道闸(背书+白名单)早写好了,一直零调用方 —— 链子跑到最后一步就断。
  # 🔴 上线方式(PM 认可):现在挂着 ?dry_run=1,**只选不做**,把名单打出来给人眼确认。
  #    确认名单对了之后分两步放开:先去掉 dry_run 只留 ?max=1 试水一轮,
  #    再把 ?max=1 也去掉(一轮最多 3 件,每客户 1 件是代码里的硬上限)。
  #    这两步各是一行改动,改在这里、看得见、能回滚。
  # 每天 09:30 UTC,排在周二 08:00 的处方之后 —— 新方案落地当天就能被执行。
  - type: cron
    name: execution-auto-run
    env: node
    schedule: "30 9 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        "https://app.magicengine.com.au/api/cron/execution-auto-run?dry_run=1"
    envVars:
      - key: CRON_SECRET
        sync: false

  - type: cron
    name: anomaly-detector-daily
    env: node
    schedule: "0 5 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/anomaly-detector \
        && curl -fsS "https://hc-ping.com/ff78126a-25e2-4a50-abcb-edbcb1dfaec9"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Daily cron health digest — every day at 6am UTC (~6pm NZST)
  # Queries cron_run_logs for the past 24h; sends Resend email only if failures found.
  # healthchecks.io: daily-cron-digest (10c101e8-05a0-4026-9531-39edddd7c74d)
  - type: cron
    name: daily-cron-digest
    env: node
    schedule: "0 6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/daily-cron-digest \
        && curl -fsS "https://hc-ping.com/10c101e8-05a0-4026-9531-39edddd7c74d"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Proposal view digest — every day at 19:00 UTC (~07:00 NZST next morning)
  # Rolls up who opened a Magic Engine sales proposal in the past 24h from
  # website_lead_events (source='proposal_tracking'), and emails the read
  # pattern to hello@magicengine.cloud. Sends nothing on a quiet day, so the
  # arrival of the email is itself the signal.
  - type: cron
    name: proposal-view-digest
    env: node
    schedule: "0 19 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/proposal-view-digest
    envVars:
      - key: CRON_SECRET
        sync: false

  # Agent Learning Rollup — DAPE W3 (spec §3.3)
  # Weekly aggregator: takes the previous ISO week (Mon 00:00 UTC → Mon 00:00 UTC)
  # of flywheel_outcomes + zhuge_feedback_events and writes ONE summary
  # preference per active client into client_learned_preferences. Next agent run
  # reads it via loadMemoryForClient so "what worked / what got dismissed last
  # week" is in the prompt without re-deriving from raw outcomes each time.
  # Runs Mon 07:00 UTC (after the Sunday/early Monday data pulls have settled).
  # Reference: docs/superpowers/specs/2026-06-08-me-dape-redefine-v0.2.md §3.3
  - type: cron
    name: agent-learning-rollup
    env: node
    schedule: "0 7 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/agent-learning-rollup
    envVars:
      - key: CRON_SECRET
        sync: false

  # Memory extractor — IMPACT 的 Tune 段（Phase 23.C）
  #
  # 把 Check 段算出来的 flywheel_outcomes 变成客户私有的学习：
  #   confirmed → client_proven_patterns（这么做有效）
  #   reversed  → client_failed_experiments（这么做反而变差）
  #   同类动作连续跑赢 3 次 → client_learned_preferences
  #   并回填 client_decision_history.outcome_verdict
  #
  # 🔴 这条路由 2026 年就写好了，但**从来没有登记过调度** —— registry.ts 和本文件
  #    里都没有，cron_run_logs 里一条运行记录都没有。后果：Check 段一直在产出
  #    结论（2026-08~09 两个月 285 条），Tune 段却停在 2026-07-15 的 21 条，
  #    53 天里没有任何一条真实结果改变过下一次判断。
  #    按 ME_PRODUCT_DEFINITION §4 的定义，Tune 没有改变后续决策就不算学习闭环 ——
  #    也就是说 IMPACT 最后一段整段没通电，而且"没通电"和"一切正常"在监控里长得一模一样。
  #
  # 排班位置不是随手挑的，这条链有先后：
  #   attribution (0 */6) 先算出 outcome → 本任务 (30 6) 抽成学习 →
  #   agent-learning-rollup (0 7 周一) 再汇总
  # 06:30 UTC 落在 attribution 06:00 那一轮之后半小时（它 maxDuration 才 300s），
  # 又在周一 07:00 的汇总之前，两头都不抢。
  #
  # --max-time 比路由自己的 maxDuration(300s) 宽：curl 先断开的话 Render 记成失败，
  # 但路由还在跑、库还在写 —— 运维照着"失败"重跑一次就是重复处理。
  - type: cron
    name: memory-extractor
    env: node
    schedule: "30 6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 360 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/memory-extractor
    envVars:
      - fromGroup: me-shared-cron-secret

  # SEO Patrol — Phase 22.E.S2b
  #   Daily SEO opportunity engine: reads keyword_snapshots + GSC snapshots,
  #   runs the rule engine, persists fresh findings, and routes the top 3 onto
  #   the SEO kanban column via 诸葛亮's persister (source='zhuge').
  #   Runs after google-data-pullback-daily (3am) so data is fresh.
  # Oztop SEO automation loop — weekly meta optimisation
  # Reads GSC opportunity keywords (pos 4-20) → AI meta → WP PATCH → work log
  # Requires: OZTOP_WP_USERNAME, OZTOP_WP_APP_PASSWORD (set in Render dashboard)
  - type: cron
    name: oztop-seo-optimizer
    env: node
    schedule: "0 5 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST \
        -H "Authorization: Bearer $CRON_SECRET" \
        "https://app.magicengine.com.au/api/cron/oztop-seo-optimizer?max=8"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly site recrawl — Sunday 01:00 UTC. Refreshes client_site_pages for
  # focus clients (seo_config.weekly_blog=true) so the patrol's link-graph
  # (R2) and blog page-context stay at most a week old.
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render dashboard.
  - type: cron
    name: site-audit-weekly
    env: node
    schedule: "0 1 * * 0"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/site-audit-weekly
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly watchdog report — 18:30 UTC Sunday = Monday ~06:30 NZST, half an
  # hour before Monday's daily to-do. Per focus client: rankings / auto
  # changes / blog / social / ads, every section freshness-stamped.
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render dashboard.
  - type: cron
    name: weekly-seo-report
    env: node
    schedule: "30 18 * * 0"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/weekly-seo-report
    envVars:
      - key: CRON_SECRET
        sync: false

  # PM daily to-do — 19:00 UTC Sun-Thu = NZ weekday mornings (07:00 NZST /
  # 08:00 NZDT). Rolling pillar theme + live counts of drafts/findings/cards
  # awaiting the PM. Route double-guards NZ weekends.
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render dashboard.
  - type: cron
    name: pm-daily-todo
    env: node
    schedule: "0 19 * * 0-4"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 120 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/pm-daily-todo
    envVars:
      - key: CRON_SECRET
        sync: false

  # ME2 Product Map — 每日全量对账 GitHub(webhook 兜底 + 未分类扫描 + 台账清理)。
  # 同步表未 apply 前会报 failed(设计行为,不许静默绿)。
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render dashboard.
  - type: cron
    name: product-map-sync-daily
    env: node
    schedule: "15 18 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/product-map-sync
    envVars:
      - key: CRON_SECRET
        sync: false
      # 未配置 = 人话摘要功能整体跳过(不影响 PR/issue facts 同步本身,route.ts 已做降级)。
      # ⚠️ 部署后需在 Render 后台给这个服务手动 link ANTHROPIC_API_KEY(跟仓库里其它
      #    需要它的服务同一个做法,sync:false 不会自动挂),否则摘要一直是"未接大模型"。
      - key: ANTHROPIC_API_KEY
        sync: false

  # CTS title executor — Monday 05:30 UTC, right after oztop-seo-optimizer.
  # GSC pos-4-20 pages → AI title/desc → narrow-lane edit of the site repo's
  # meta data file → ONE weekly PR (human merges, client repo CI gates).
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render dashboard.
  - type: cron
    name: cts-seo-optimizer
    env: node
    schedule: "30 5 * * 1"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/cts-seo-optimizer
    envVars:
      - key: CRON_SECRET
        sync: false

  # Weekly blog draft — Tuesday 3am UTC (after Monday keyword snapshots +
  # Monday AI tracker runs, so both topic-selection legs are fresh).
  # One auto-topic draft per client with seo_config.weekly_blog=true.
  # Drafts await human review in Blog Studio — never auto-publishes.
  # ⚠️ sync:false does NOT auto-fill: after first deploy, link the
  #    me-shared-cron-secret env group to this service in the Render
  #    dashboard, or every run 401s silently (daily-cron-digest lesson).
  # Reference: ROADMAP.md 22.E.S16
  - type: cron
    name: blog-weekly
    env: node
    schedule: "0 3 * * 2"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/blog-weekly
    envVars:
      - key: CRON_SECRET
        sync: false

  # Reference: ROADMAP.md § Phase 22.E · docs/seo-sop-implementation-design.md
  # healthchecks.io: seo-patrol-daily (db58fff5-fd32-45e0-a7fd-b067bede6689)
  - type: cron
    name: seo-patrol-daily
    env: node
    schedule: "0 4 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/seo-patrol-daily \
        && curl -fsS "https://hc-ping.com/db58fff5-fd32-45e0-a7fd-b067bede6689"
    envVars:
      - key: CRON_SECRET
        sync: false

  # Social comment auto-reply — every 30 min for enabled clients.
  # Pulls recent FB Page comments → classify → guardrailed auto reply (public +
  # optional DM) / hide spam. 30-min cadence keeps DM private_replies inside
  # Meta's 7-day window. Kill switch: SOCIAL_COMMENT_AUTOREPLY_KILL=1
  # Reference: ROADMAP.md P20.D (social pillar / DAPE Execution)
  - type: cron
    name: social-comment-autoreply
    env: node
    schedule: "0 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 900 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/social-comment-autoreply
    envVars:
      - key: CRON_SECRET
        sync: false

  # ── 内容工厂:发布最后一米 ────────────────────────────────────────────────
  # 这两个 cron 的 route 早已实现(PR #592)但从未注册,approved 的成片因此停在
  # 「已通过·待发布」永远不动 —— 工厂链路的最后一段一直是断的。
  #
  # 🔴 安全阀:未设 FACTORY_PUBLISH_LIVE=true 时**只发草稿**(video_state=DRAFT,
  #    不公开),供 PM 验格式。验完 PM 显式 go 才在 Render 设 true 真发客户主页(不可逆)。
  # 发布正文在 publish-worker 内另有红线闸(scanPublishCaption),命中即终态不发。
  # 讲课片发布也在这条 worker 里顺带跑(runLecturePublishSweep),**不另开 cron**:
  # 新建的 cron 服务不会自动拿到 CRON_SECRET(sync:false 只声明不填值),建了也只是每轮 401
  # 静默失败 —— 2026-08-04 实测:排队的片等了 14 分钟没动,cron_run_logs 里连一条运行记录都没有。
  # 挂在已经配好密钥、稳定在跑的 worker 上,就不需要任何人去后台补配置。
  # 讲课片发布也在这条 worker 里顺带跑(runLecturePublishSweep),**不另开 cron**:
  # 新建的 cron 服务不会自动拿到 CRON_SECRET(sync:false 只声明不填值),建了也只是每轮 401
  # 静默失败 —— 2026-08-04 实测:排队的片等了 14 分钟没动,cron_run_logs 里连一条运行记录都没有。
  # 挂在已经配好密钥、稳定在跑的 worker 上,就不需要任何人去后台补配置。
  # 2026-08-16 [OPS] 降频 */10 → 每小时第 5 分：工厂未开工，30 天每轮都领 0 条。
  # 注意这条一轮只领 1 条 —— 每小时一轮 = 发布吞吐上限 24 条/天。工厂真开工、
  # 一天要发的量超过这个数时，必须先把它改回 "*/10 * * * *" 再开工。
  - type: cron
    name: factory-publish-worker
    env: node
    schedule: "5 * * * *"   # 原 "*/10 * * * *"：一轮领 1 条(FB 视频上传慢),10 分钟一轮够用
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/factory-publish-worker
    envVars:
      - key: CRON_SECRET
        sync: false

  # 自动排产 — 工厂的「开始按钮」。整条链是 下单→做片→审片→发布,前三段都在跑,
  # 但此前**没有任何东西会自动创建工单**(唯一入口 /api/factory/signals 无调用方),
  # 所以工厂修得再好也不会自己动。每天给开了 auto_order_enabled 的客户各下一单。
  # 🔴 按客户显式开关、默认关(自动下单=自动花钱)。余额下限/日单量/日花费上限
  #    全部沿用 strategist 既有护栏,不另开口子。dedupe_key 按 NZ 日切天 = 一天一单。
  # 20:00 UTC ≈ 08:00 NZST,落在 NZ 当天早上,当天有一整天可以出片+审片。
  - type: cron
    name: factory-order-scheduler
    env: node
    schedule: "0 20 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 120 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/factory-order-scheduler
    envVars:
      - key: CRON_SECRET
        sync: false

  # 🔴 已暂停(2026-09-08,PM 拍板「停抓图」)。原意是给出片补底图,但把抓来的图变成
  #    可用底图的那一步(transformStockImages / stock-transform)全仓零调用方 = 死代码,
  #    抓来的原图被 evaluate 的「只收 AI 改过的图」硬闸挡在成片外,谁也不消费。结果这条
  #    cron 每周花 Apify 的钱抓一堆没人用的 Pinterest 图。停掉止损。路由侧也加了空转兜底。
  #    恢复时三件一起做:本块取消注释 + registry.ts 那行加回来 + 先把 stock-transform 接进
  #    调用链(否则恢复了还是白抓)。
  # - type: cron
  #   name: factory-stock-refill
  #   env: node
  #   schedule: "0 19 * * 1"
  #   buildCommand: ""
  #   startCommand: |
  #     curl -fsS --max-time 300 -H "Authorization: Bearer $CRON_SECRET" \
  #       https://app.magicengine.com.au/api/cron/factory-stock-refill
  #   envVars:
  #     - key: CRON_SECRET
  #       sync: false

  # 卡死回收:publishing 超 10 分钟(进程被杀/超时)→ 退回 approved 等下轮重领。
  # 没有它,一次崩溃就会让那条工单永远卡在 publishing,没人能再领。
  # 2026-08-16 [OPS] 降频 */15 → 每小时第 25 分：工厂未开工，30 天零回收。
  # 排在 worker(第 5 分)之后 20 分钟 —— 崩掉的工单当轮就退回 approved，下一轮
  # worker 正好能领走，不会跨两小时。回滚：schedule 改回 "*/15 * * * *"。
  - type: cron
    name: factory-publish-sweeper
    env: node
    schedule: "25 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 120 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/factory-publish-sweeper
    envVars:
      - key: CRON_SECRET
        sync: false

  # 客户私信 → CRM。PR #652/#653 把接口合进来了,但没有人调用它们,
  # 「客户消息」页会永远是空的 —— 这里补上调用方。
  #
  # 🔴 **写需求卡那一步 2026-09-07 挪走了,别再往这里加第二条 curl。**
  #    原来这里是 `curl 私信同步 && curl 写需求卡`。意图是对的 ——「同步没跑成就别
  #    拿半截数据写卡给销售看」—— 但 `&&` 守的是 curl 的退出码,而退出码回答的是
  #    「网关有没有在超时前把响应给我」,不是「同步有没有跑完」。
  #    同步的服务端耗时 2026-08-17 起从约 25 秒跳到约 140 秒,网关约 125 秒掐断返 524,
  #    curl -f 退出码 22 → && 短路 → **写卡那条从 2026-08-23 起一次都没执行过**,
  #    销售的客户需求卡停更 14 天,而监控上「私信同步每小时正常」。
  #
  #    现在:同步跑到最后一行发一张 `me/messenger.sync.completed` 条子,
  #    Inngest 上的 `cloud-messenger-brief-after-sync` 收到就写卡(还分段跑,每段约 40 秒,
  #    不再踩网关那条线)。判据从「curl 的退出码」换成「服务端自己的回执」。
  #    整段来龙去脉见 src/lib/messenger/sync-completed-event.ts。
  #
  #    ⚠️ Render 不是 Vercel,Inngest 没有自动同步:改了那个函数之后必须重新 Sync 一次
  #       `curl -sS -X PUT https://app.magicengine.com.au/api/inngest`(返 modified:true 才算数),
  #       否则它**安静地不跑**。这条链路已登记进 CRON_REGISTRY,真没跑起来健康检查会喊。
  #
  #    `/api/cron/messenger-brief-hourly` 路由保留,用于单独手动补触发。
  #
  # 只同步 clients.facebook_page_id 有值的客户(设置页里选主页才会有值),
  # 所以新增客户不需要动这里。
  # max-time 跟接口的 maxDuration 对齐(600s)再留一点余量。
  #
  # ⏸️ **降频到每 2 小时的改动已从本 PR 抽出，暂不落地**（issue #1581，H14 原计划）：
  #    方案是"`/api/webhooks/meta/messenger` 上线后，实时收信改由那条 webhook 负责，
  #    这条任务降级成兜底"，但截至提交这版代码，Meta 生产企业验证仍未通过
  #    （issue #1299 仍是 OPEN），webhook 订阅还打不开。这条 cron 现在仍是**唯一在收
  #    真实消息的路径**——这时候降频 = 私信进系统的延迟从 ≤1h 变成 ≤2h，是纯粹的倒退，
  #    没有任何东西在补它腾出来的窗口。CTS/NAL 广告私信留资流程正在依赖这条延迟
  #    （2026-09-13 CTS 广告顾问窗口反馈）。**保持每小时不变，等 #1299 关闭、webhook
  #    真正收到生产流量、并用真实数据验证过之后，再单独一个 PR 改这条 schedule。**
  #    `src/lib/messenger/sync.ts` 的 `storeConversation` 防覆写改动（"只在本轮数据比
  #    已存的新时才覆盖 participant_name/last_message_at/last_message_from"）不受这条
  #    影响，正常保留——那是数据正确性修复，跟频率无关，任何频率下都成立。
  #
  # ⚠️ 这条 curl **本来就会超时**(网关约 125 秒掐断返 524,而接口跑约 140 秒),
  #    Render 的运行日志里会看到 `curl: (22) ... error: 524` / `Exited with status 22`。
  #    服务端每一轮都跑完了 —— 判据看 cron_run_logs 里 messenger-sync-hourly 那一行,
  #    不看这条 curl 的退出码。别再往这个退出码上挂任何下游步骤。
  - type: cron
    name: messenger-hourly
    env: node
    schedule: "10 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/messenger-sync-hourly
    envVars:
      - key: CRON_SECRET
        sync: false

  # 公司邮箱 → CRM：**不再是一条独立的 cron 服务**（2026-08-03）。
  #
  # 它原来在这里是一条新增的 cron 服务，结果一次都没跑过 —— Render 的蓝图对
  # 新增服务不会自动生效，要有人进后台点一次 Apply，而这件事不会报任何错。
  # 现在挂在上面 messenger-hourly 那条**已经在跑**的任务里（见那个路由的文件头）。
  # `/api/cron/mailbox-sync` 路由保留，用于单独手动触发。
  #
  # 以后再加渠道同步，优先挂进已有任务，别新开服务 —— 新服务 = 一个人工步骤。

  # Facebook 即时表单 → CRM。之前这条路只有人手一条(导 CSV 跑 import-cts-fb-leads.ts),
  # 2026-07-30 查出来的后果:CTS 的 CRM 里最后一个新人停在 7/25,而 Meta 后台
  # 7/26–7/30 又进了 27 个人,销售的「今天该联系谁」里一个都没有。
  #
  # 只同步 clients.facebook_page_id 有值的客户(跟私信同步同一个开关),
  # 所以新增客户不需要动这里。
  # 跑在每小时第 25 分钟:避开整点那一堆日更 cron,也避开第 10 分钟的私信同步
  # ——两条都会调 Meta Graph,岔开跑不容易撞上 Meta 的限流。
  - type: cron
    name: meta-leads-hourly
    env: node
    schedule: "25 */2 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/meta-leads-sync
    envVars:
      - key: CRON_SECRET
        sync: false

  # 广告上线闸门 —— 每天把所有**在投**的广告组回读一遍，不管广告是谁建的。
  #
  # 为什么触发点是「每天扫」而不是「ME 建广告时」(2026-08-05 子牙复审)：
  # 闸门原来只挂在 ME 自己的建广告入口上,而 2026-08-04 真正得罪 Boris/Richard/Jude
  # 那批广告是在 Meta 后台直接建的 —— 规则写得再对也永远轮不到它说话。
  # 按账户扫在投广告组之后,ME 建的 / FDE 手建的 / 客户自己建的 / Meta 自动改过的,
  # 一视同仁。
  #
  # 查出来的 blocker 进「今日待办 → 需要你动手」,不留在日志里。
  # 跑在 20:40 UTC(NZ 早上 8:40 左右):人上班第一眼就能看到,而不是等到花完一天钱。
  # ⚠️ 2026-08-16 [OPS]:上面这句「部署后必须去 Render 后台 link」当初**没人去做**,
  #    结果它从上线起 30 天零运行记录 —— 每天 20:40 准点 401,悄无声息。
  #    现在改成 fromGroup 自动挂密钥,不再依赖任何人记得去点后台。
  - type: cron
    name: ad-readback-sweep-daily
    env: node
    schedule: "40 20 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/ad-readback-sweep
    envVars:
      - fromGroup: me-shared-cron-secret

  # 广告设置快照（ads IMPACT 阶段 1，设计 §2.1 / §14 M1）—— 每 3 小时只读抓一次 Meta
  # 账户/系列/广告组/广告/受众设置，只在设置变化时记一行、另每天一次。
  # 只读 Meta，不花钱（Graph 读接口免费，只占 API 配额）。共用账户只记账户级。
  # 依赖 migration 20260914000001（ad_entity_snapshots 表）先 apply；表不在时每轮写入失败、
  # 运行记录里会报错，不影响其它任务。
  - type: cron
    name: ad-entity-snapshot
    env: node
    schedule: "30 */6 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/ad-entity-snapshot
    envVars:
      - fromGroup: me-shared-cron-secret

  # 🔴 2026-09-02 退役：content-factory-render-worker(旧 Render 云端拼片管线)。
  #    content_factory_render_jobs 表最后一条记录 2026-08-19，两周零产出，讲课式确认
  #    当前无客户在用(PM 口径)。CTS/Oztop 的广告成片(Winner Intake)确实已转到本机
  #    scripts/factory-worker(content_work_orders 队列)；但这条 render.yaml 服务背后
  #    连的是"确认选题"这个通用入口，服务普通内容(非讲课式/非LinkedIn)，这类内容
  #    目前**没有**自动接班的管线——工单不是从这个入口建的，退役后这类内容出片需要
  #    人工另外处理，不是"自动切到新线了"，见 [postId]/route.ts 里的对应说明。
  #    旧代码(render-assemble.ts / render-pipeline.ts / lecture-render.ts /
  #    render-queue.ts / scripts/render-worker/)本次先留仓库不删(删除类 Bash 操作被
  #    session 权限拦下)，只摘掉这个 Render 服务和调用它的入口，表数据不动。

  # 邮件反应 → CRM：谁收到了、谁打开了、谁点了行程链接。
  #
  # 这条接口 2026-07 就写好了（PR #686），但**从来没被注册**，一次都没跑过 ——
  # 2026-08-01 体检才发现：销售看不到「谁点了行程链接」，只能按「我们打没打过他」排序。
  #
  # ⚠️ 密钥用 fromGroup 自动挂，**不要**改回 `sync: false`：
  # 手挂密钥是这个仓库反复踩的坑 —— meta-leads-hourly 上线当天就因为手挂的值跟接口
  # 对不上，连着两小时 401、库里一条运行记录都没有；更早还有一条 cron 因此哑了 51 天。
  # 环境变量组 `me-shared-cron-secret` 已存在（Render 的 Link Environment Group 里能选到）。
  # 万一 Render 不认这个写法，回退方案是 `- key: CRON_SECRET` + `sync: false`，
  # 然后**必须**手动 link 同一个组 —— 别只填一个手打的值。
  - type: cron
    name: mailchimp-activity-daily
    env: node
    schedule: "40 4 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/mailchimp-activity-sync
    envVars:
      - fromGroup: me-shared-cron-secret

  # 付款邮件 → Mailchimp `paid_customer` 标签（付了钱的人自动退出群发名单）。
  #
  # 在这之前这件事完全是手工的：CTS 那 19 个 paid_customer 标签是 2026-08-30
  # 有人翻 info@ 邮箱手打的，之后再没人打过。代价当天就付了 —— 9/1 那次 192 人
  # 的群发混进 2 个已付款客户，其中一个是 Baker 亲口回过
  # 「your payment has been received in full」的 Nikki Smith。
  #
  # 只自动做「我们自己确认收款」那一档；客人自己说付了的一律不自动打，
  # 攒进 summary.needsReview 由今日待办下发给人点一下（判据见 lib/mailchimp/paid-signal，
  # 那里有一条红线：这个邮箱里催款信和收款信长得几乎一样，判错会把正在谈的客人停掉）。
  #
  # 排在 activity-sync（4:40）之后 —— 那条把邮件反应搬进 CRM，这条改标签，
  # 两条都碰 Mailchimp，错开避免同一分钟撞 API 限流。
  #
  # 补历史：手动 POST 带 ?days=N（如 ?days=365），跟每天对账同一套代码。
  #
  # ⚠️ 密钥用 fromGroup 自动挂，**不要**改回 `sync: false`（同上一条的坑）。
  - type: cron
    name: mailchimp-paid-tagging-daily
    env: node
    schedule: "10 5 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 620 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/mailchimp-paid-tagging
    envVars:
      - fromGroup: me-shared-cron-secret

  # ─────────────────────────────────────────────────────────────────────────
  # Team Working Memory 兜底清扫 — 每 30 分钟
  #
  # 本机 hook 在会话结束时会自己触发提炼，这条 cron 是兜底：
  # 笔记本睡了 / 断网 / 进程被杀导致那一枪没打中的会话，在这里补上；
  # 顺带把被推翻的教训撤下、连续砸锅的套路下架（PM 选了全自动，收拾也得机器做）。
  #
  # ⚠️ 部署后必须在 Render 后台把这个 cron 关联到 me-shared-cron-secret 环境变量组。
  #    sync:false 不会自动填值 —— daily-cron-digest 就是因为这个哑了 51 天没人发现，
  #    而 digest 只报「失败」不报「没跑」，所以静默失效不会有任何告警。
  - type: cron
    name: team-memory-sweeper
    env: node
    schedule: "*/30 * * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 300 -X POST -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/team-memory-sweeper
    envVars:
      - key: CRON_SECRET
        sync: false

  # ─────────────────────────────────────────────────────────────────────────
  # ME product progress → LinkedIn — Monday + Thursday, ~9am NZ time
  #
  # No-ops unless LINKEDIN_PROGRESS_POST_ENABLED=true — this repo's web
  # service isn't Blueprint-managed (real prod is `crazycontent`, created by
  # hand in the Render dashboard), so this flag can't be declared here.
  # Absent = undefined !== 'true' = disabled by default, no dashboard action
  # needed to ship dark. PM sets LINKEDIN_PROGRESS_POST_ENABLED=true directly
  # on crazycontent's Environment tab when ready to turn it on.
  #
  # Schedule note: NZ is UTC+12/+13 (DST), so "Monday/Thursday 9am NZ" falls on
  # the PREVIOUS UTC calendar day (Sunday/Wednesday). 20:30 UTC lands at
  # roughly 8:30am NZDT–9:30am NZST year-round — good enough for a personal
  # LinkedIn post, not worth DST-exact scheduling for this.
  - type: cron
    name: linkedin-progress-post-mon
    env: node
    schedule: "30 20 * * 0"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/linkedin-progress-post-mon
    envVars:
      - fromGroup: me-shared-cron-secret

  - type: cron
    name: linkedin-progress-post-thu
    env: node
    schedule: "30 20 * * 3"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 60 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/linkedin-progress-post-thu
    envVars:
      - fromGroup: me-shared-cron-secret

  # Magic Insight（内部代号 market-intel）—— PM 每日全球 AI / 数字营销资讯雷达。
  #
  # 设计见 docs/specs/2026-08-20-market-intel-digest-design.md（v2，已过子牙 +
  # 魏征设计审）。18:00 UTC ≈ 06:00 NZST / 07:00 NZDT，两季都落在清晨；PM 明确
  # 不设硬性时间要求，可按体验微调。密钥直接走 fromGroup，不留"记得手动 link"
  # 这一步——daily-cron-digest 就是靠人记住这件事，51 天没人发现地哑了。
  #
  # ⚠️ 尚未申请 healthchecks.io 监控 ID，先不接 ping；有账号权限的人申请后
  #    在 startCommand 末尾按现有条目的格式加一行 `&& curl -fsS "https://hc-ping.com/<ID>"`。
  - type: cron
    name: market-intel-daily
    env: node
    schedule: "0 18 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 180 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/market-intel-daily
    envVars:
      - fromGroup: me-shared-cron-secret

  # 清一次 cron_run_logs 的旧运行记录。
  #
  # 🔴 这活儿以前是表上的 AFTER INSERT 触发器干的 —— 每天 400~600 次插入就跑
  #    400~600 次全表 DELETE，并发插入还会死锁，而死锁会让**插入本身失败**。
  #    运行记录写不进去恰好是这套监控最不能出的事，所以搬成每天一次的独立任务
  #    （触发器在 supabase/migrations/20260907000001 里拆掉了）。
  #
  # 保留规则一行没改，还是生产上那两条：7 天以上的空记录 + 180 天以上的全部。
  # 16:50 UTC ≈ NZ 凌晨 04:50，避开整点那批任务的插入高峰。
  # 密钥走 fromGroup，不留「记得手动 link」这一步。
  - type: cron
    name: cron-run-logs-cleanup
    env: node
    schedule: "50 16 * * *"
    buildCommand: ""
    startCommand: |
      curl -fsS --max-time 180 -H "Authorization: Bearer $CRON_SECRET" \
        https://app.magicengine.com.au/api/cron/cron-run-logs-cleanup
    envVars:
      - fromGroup: me-shared-cron-secret

  # 客人来信没人回 —— 每天早上给销售发一封汇总信（测试期发到 ME 自己的信箱）。
  #
  # 判据 src/lib/crm/email-reply-due.ts（超时 24 小时，PM 2026-09-03 拍板），
  # 文案 src/lib/crm/email-reply-digest.ts（主题和正文都带「【测试功能】」）。
  #
  # 时区换算：NZ 冬令时 NZST = UTC+12，所以 **UTC 20:00 = 次日 NZ 08:00**。
  # 夏令时 NZDT = UTC+13（9 月底～4 月初）时同一条排班落在 NZ 09:00 ——
  # 「上班后第一件事」这个位置两季都成立，不为夏令时另开一条。
  #
  # 密钥直接走 fromGroup，不留「记得手动 link」这一步 —— daily-cron-digest 就是
  # 靠人记住这件事，51 天没人发现地哑了。
  #
  # 🔴 2026-09-03 PM 拍板**暂停调度**（上线当天，一封都还没发出去过）。
  # 原因：拿生产库核对名单发现，现有排除规则只挡「自己人域名」和「同行域名」，
  # 挡不住陌生公司群发的推销。CTS 当天 32 条候选里噪音占七成，而排序按「等最久」，
  # 等最久的恰好是没人理的营销邮件 —— 前 10 条里 8 条是垃圾，真正欠回复的客人
  # （Julie Cawte 的发票、Torran Lepper 的日本行程）反而被挤出名单。
  #
  # 判据和文案本身没问题，差的是一层噪音过滤。加好、并用生产数据验证准确率之后，
  # 把下面整段取消注释、同时把 EMAIL_REPLY_DIGEST_ENABLED 设成 true（两道闸都要开）。
  #
  # - type: cron
  #   name: email-reply-digest
  #   env: node
  #   schedule: "0 20 * * *"
  #   buildCommand: ""
  #   # --max-time 要比路由自己的 maxDuration(300s) 宽：curl 先断开的话 Render 把这次
  #   # 记成失败，可路由还在跑、信照样发出去 —— 运维照着「失败」重跑一次就是重复发信。
  #   # 重跑本身另有幂等闸挡着（见路由里的 RESEND_GUARD_HOURS），这里只是别去主动制造它。
  #   startCommand: |
  #     curl -fsS --max-time 360 -H "Authorization: Bearer $CRON_SECRET" \
  #       https://app.magicengine.com.au/api/cron/email-reply-digest
  #   envVars:
  #     - fromGroup: me-shared-cron-secret
